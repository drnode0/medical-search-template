# 双輪サイクル 実行リスト（セッション1本＝1枚）

日付: 2026-09-03
正: `docs/superpowers/specs/2026-09-03-cycle-skeleton-design.md`（骨組み・継ぎ目・パーツ台帳・仕組み台帳・深掘り）。
この文書は、設計書を読まなくてもセッションを開けるように、部品ごとに「着手条件・決めること・作るもの・守る継ぎ目・借りるパーツ・完了条件・最初の一言」だけを抜いたもの。
矛盾があれば設計書が勝つ。

## 進め方の約束（全セッション共通）

- 1部品＝1セッション。同じ main に2本を走らせない。worktree を切る（`superpowers:using-git-worktrees`）
- 設計が要る部品は `superpowers:brainstorming` → 設計書 → `superpowers:writing-plans` → 実装。要らない部品は `writing-plans` から
- 実装はサブエージェント方式（`superpowers:subagent-driven-development`）。実装 Sonnet 5・レビュー Opus 5
- テストは実データ（`.preview/` の主張コーパス、受付DBの実物、本番 cron の手動実行）
- push は毎回オーナーの承認。公開リポなので事業数値を書かない
- 完了条件は「オーナーが本番で実測した」。実装が終わっただけでは閉じない
- セッションの終わりに `/shime`。引き継ぎは `~/.claude/handoff/app.md`

## 順番と依存

| 順 | 部品 | 着手条件 | 設計 | モデル |
|---|---|---|---|---|
| 0 | DESIGN.md | いつでも | 不要 | Sonnet 5 |
| 2 | 回答通知の仕上げ | いつでも | 不要 | Sonnet 5 |
| 1 | 読む画面の「残す」「読んだ」 | Recall でサブスク同期を1回押し、残す→確かめる→覚えた の一周が本番で通っている | 不要 | Sonnet 5 |
| R | Recall への申し送り（別セッション） | いつでも（Recall セッション側で） | 小 | Opus 5 |
| 3 | 聞ける棚の段0＋段2 | Recall セッションが同期側のコードを触り終えている | 要 | Opus 5 |
| 4 | Notion への一方向落とし | 3で「正本主張ID」の書き手が決まっている | 要 | Opus 5 |
| 5 | 段1（AI に組ませる） | 3が本番で動いている | 要 | Opus 5 |
| 6 | 今日の1問の撤去＋週1の便り | 1〜3をオーナーが一周した後 | 小 | Sonnet 5 |
| 7 | 記載フォーム＋昇格描画 | 1の後。/admin の個人リーダー計測を見てから | 要 | Opus 5 |
| 8 | 改訂の旗 | 3の後。改訂が1件起きたときを目安 | 要 | Opus 5 |
| V | 見た目の比較（現行 vs Node Field 寄せ） | 3が着地し、オーナーが一周した後 | 要 | Opus 5 |

---

## 部品0 DESIGN.md

- **目的**: 以後の実装セッションが画面を足すたびに見た目が揺れないよう、現行の見た目を1枚に写す
- **作るもの**: リポジトリ直下 `DESIGN.md`。色（常盤 #196B4F と tailwind の brand パレット）、書体（Noto Sans JP）、字の大きさの段、余白、角丸、ボタンとカードの型、lucide アイコンの使い方、ダーク（`.dark` 基準）。中身は現行の写しで、新しい見た目を決めない
- **参照**: Refero Styles の DESIGN.md の形式。`emilkowalski/skills` の apple-design は読んでよいが取り込まない
- **完了条件**: main に入り、部品1のセッションが冒頭で読める
- **最初の一言**: 「`docs/superpowers/plans/2026-09-03-cycle-skeleton-sessions.md` の部品0を superpowers:writing-plans で計画し、実装まで進めてください。worktree を切り、push は承認を取ってから」

## 部品2 回答通知の仕上げ

- **目的**: 正本になった事実が依頼者にメールで届く経路を閉じる（外の輪の決定7）
- **状態**: 実装済み・未コミット。`src/lib/cq-answer-notify.ts`、`src/app/api/cron/cq-answer-notify/route.ts`、テスト、`cq-mine.ts` の「対応済み」判定、`vercel.json` の cron（10:00 UTC）。テスト 17 件・全体 1683 件通過、tsc クリーン。Vercel に必要な env（CRON_SECRET・RESEND_API_KEY・RESEND_FROM・CQ_INTAKE_*）は存在を確認済み
- **やること**: 未コミット分を1コミットにする → push（承認）→ デプロイ後に `CRON_SECRET` 付きで1回手動実行し `mailed`／`skipped` を見る
- **守る継ぎ目**: 継ぎ目5の「正本主張ID」対応は部品3で入る。ここではメールだけ（従来の振る舞い）
- **完了条件**: 本番で1回実行し、応答 JSON の件数をオーナーが確認した
- **最初の一言**: 「実行リストの部品2を進めてください。main の未コミット変更（cq-answer-notify・cq-mine・vercel.json）を確認してコミットし、push は承認を取ってから。デプロイ後の手動実行の手順を出してください」

## 部品1 読む画面の「残す」と「読んだ」

- **目的**: 内の輪の入口を読む画面に付ける
- **着手条件**: Recall でサブスク同期を1回押して `recall_claims` に主張が入り、残す→確かめる→覚えた の一周が本番で通っている
- **作るもの**: サブスクのアプリ内リーダー（`ReaderBody`・`ReaderSpread`）の主張行に「残す」の切替（長押しでも効く）。節末に「この節を読んだ」の明示ボタン。既存 API `/api/recall/keep`・`/api/recall/read` を呼ぶ。`hasFeature('recall')` が偽なら描かない
- **決め済み（変えない）**: 「残す」を置く行は `extract-claims.ts` の `splitClaim` が主張と判定する行だけ。`claimIdOf(ページID, 本文)` を共有関数で計算し `/api/recall/claims` の一覧と突き合わせ、無い行には置かない。節番号は `sec{n}`。スクロール自動判定なし。端末ローカルの読了水位（`ReaderMarksProvider`）と統合しない。一括操作なし
- **借りるパーツ**: 長押しで印（Matter）
- **完了条件**: オーナーが本番で記事を読み、主張行で「残す」→ 球に灯る → 節末「読んだ」→ 球に灰青で灯る、を確認
- **最初の一言**: 「実行リストの部品1を superpowers:writing-plans で計画し、superpowers:subagent-driven-development で実装してください。設計書の部品1の要点と継ぎ目1〜3・10を守ること。worktree を切り、push は承認を取ってから」

## R Recall への申し送り（Recall セッションで扱う）

- **明るさを「思い出せる確率」に**: R(t) = (1 + (19/9)·t/S)^(-1)、S は現在の間隔日数。段は固定のまま状態層の式だけ変える。離脱閾値も引き直す
- **「覚えた／まだ」を3択に**: 「もう一度／来週／しばらく先」（日数ラベル。Readwise Mastery の型）
- **伏せ字の数を記憶度で変える**: 暗いほど穴1つ、明るいほど上限3。**実装前に特許 JP7495135B2（Monoxer）の請求項を読む**
- **想起カードの手がかり**: 冒頭の一句（最初の読点まで）を表に出す
- ~~**`due_at` が null の行**: 「残したが未開封」として候補から外し、球では「残した」の色で灯す~~ 取り下げ（2026-09-05 提案006の裁定1。cron は `recall_progress` に書かない）
- **最初の一言**: 「Recall の設計書に、実行リスト（`docs/superpowers/plans/2026-09-03-cycle-skeleton-sessions.md`）の R 節の5点を反映する計画を立ててください。特許の確認は請求項を読んだ結果を先に報告すること」

## 部品3 聞ける棚の段0＋段2（設計セッション → 実装）

- **目的**: 疑問が棚に当たり、無ければ依頼になり、正本になって戻る経路を作る
- **着手条件**: Recall セッションが同期側（主張の抽出・穴・承認）のコードを触り終えている
- **設計セッションで決めること**
  1. 段0の検索方式: 埋め込みの提供元（OpenAI 3-small／Gemini／Cohere embed-v4 を日本語の医学文で実測）＋日本語全文検索（PGroonga）の併用。列は `recall_claims` に足す。更新は同期の中
  2. 「棚に無い」の閾値（類似度と件数。Consensus の「材料が足りなければ出さない」の型）
  3. 「正本主張ID」の書き手（管理画面の操作か Notion 手入力か）
  4. 制作工程との受け渡し: CQ スキルが受付DBの依頼を入口として読む手順と、正本化で「対応済み」＋「正本主張ID」を書き戻す手順（スキルの中身は変えない）
  5. 依頼画面の期待値の文言（「平均○日・正本化率○%」の枠。実測後に数を入れる）と、「対応不要」の理由表示
  6. 依頼の種別に「訂正」（この主張は違う）を足す。受付DBの既存列で足りるか
  7. 依頼フォームの必須項目（背景・状況、何を調べたか。Antaa の質問テンプレの型）
  8. 空白候補ビュー（受付DBの1ビュー。段0結果＝該当なしで絞る）
  9. 音声入力を窓に置くか（転写→疑問文の形に整えるだけ。整形なしも選べる）
  10. 段0の結果に出す検証者名と検証日（データの有無を確認）
  11. 「棚に無ければ答えない」をヘルプに仕様として書く文言
- **作るもの**: 検索タブの窓の上に主張の段（あるときだけ開く・折りたたみ）、各主張に「残す」「この節を読む」「関連する主張3件」、自分が残した主張の最上位表示と印、「棚に無い」の1行、結果末尾の「作者に検証を依頼」、受付DBの列4つ（段0結果／段0主張ID／正本主張ID／見送りの理由）、回答の着地画面（依頼者がそこで「残す」。cron は `recall_progress` に書かない。2026-09-05 裁定1）、`ask_shelf` フラグ（`ASK_SHELF_EMAILS`・フォールバック無し）
- **守る継ぎ目**: 1・2・4・5・6・7・8・9
- **オーナー作業**: `ASK_SHELF_EMAILS` にオーナーのメール、マイグレーション 0030 を流す（PGroonga と 0031 は任意。埋め込みは採らないと 2026-09-05 に決めたので pgvector と API キーは不要）
- **完了条件**: オーナーが本番で疑問を書き、段0が主張を返す／「棚に無い」を出す → 依頼 → 制作工程で正本化 → 同期 → メールとプッシュが届き、着地画面で「残す」を押すと球に灯る（期限は翌日）
- **最初の一言**: 「実行リストの部品3を superpowers:brainstorming で設計してください。設計書の部品3の要点・継ぎ目・仕組み台帳・深掘りの 1・2・14・15・19・20 を入力にすること。設計が承認されたら writing-plans へ」

## 部品4 Notion への一方向落とし（設計セッション → 実装）

- **目的**: 残した主張が自分の Notion に残る
- **着手条件**: 部品3で「正本主張ID」の書き手が決まっている
- **設計セッションで決めること**
  1. DB の自動作成の流れ（既存 `/api/notion/create-db` の流用範囲）と、作成済み DB の ID と **`data_source_id`** の保存先（`user_settings` の暗号化設定）
  2. Notion API の世代（2022-06-28 固定のままか、2025-09-03 版へ上げるか。参照実装は SUBSCreen）
  3. 書き込みのタイミング（残した直後か、まとめてか）と失敗時の再送。**復号の範囲もここで決める**（提案010 判断6・2026-09-06 オーナー裁定「部品4で決める」）。
     いま利用者トークンをサーバーで復号するのは4経路だけ（設定同期・個人画像・OAuth claim・/admin 台帳）で、いずれも本人のリクエストの中。
     cron でまとめて書くと、全利用者の `user_settings` を順に復号する経路が新しくできる。増やすなら、増やすと決めた記録をこの設計に残す
  4. 1主張＝1行（ページ）。列は 主張／出典／分野／残した日／自分の一言／状態／MediNode ID。MediNode ID で検索して無ければ作成、あれば状態列だけ更新（Screvi の「作り直さない」）
  5. リレーション列は v1 で使わない（Tally が非対応にしている壁）
  6. OAuth の `bot_id` を主キーとして持っているか（RandoMemory の型。未確認）
  7. Notion 未接続の人の出口（残した主張の Markdown 書き出し）。**あわせて「自分用 CQ」の MediNode 側の保存先をここで設計する**
     （提案010 判断3・2026-09-06 オーナー裁定で採用）。現行は `create-cq` が個人 Notion にページを1枚作るだけで、
     未接続の人は自分用に残す先が無い（外の輪 決定5「Notion 接続は必須にしない」と食い違う唯一の箇所）。
     表は単独で起こさず、この項目か部品7の記載フォームのどちらかに寄せる（入口を2つ作らない）
- **守る継ぎ目**: 1・2・11
- **完了条件**: オーナーが本番で「残す」を押し、翌朝までに自分の Notion の DB に1行入り、確かめるの結果で状態列が変わる
- **最初の一言**: 「実行リストの部品4を superpowers:brainstorming で設計してください。設計書の部品4の要点・継ぎ目・深掘りの 6・7・13 を入力にすること」

## 部品5 段1（設計セッション → 実装）

- **目的**: 棚に無いとき、ボタンで AI に組ませ、未検証と分かる形で返す
- **着手条件**: 部品3が本番で動いている
- **設計セッションで決めること**
  1. モデルと費用の記録（`claude-api` スキルで現行のモデル ID と価格を確認）
  2. **Claude Citations API** を使い、段0で引いた主張を「1主張＝1文書」で渡す。引用の付いた範囲＝主張由来、無い範囲＝由来なし。中間状態（出典はあるが位置不明）の扱い
  3. 表示規約（全体に「未検証」、引用チップ、由来なし）。医療シミュの「AI 生成・未監修」の明示文の型
  4. 保存先のテーブルと、**同じ問いが正本化されたら正本を先に出す**仕組み（Doximity の型）
  5. 無料回数（1日 n 回。Upstash）と、閉じている利用者を処理に入らせない判定
  6. Notion 接続済みなら部品4の経路で落とす
- **守る継ぎ目**: 1・2・6
- **オーナー作業**: `ANTHROPIC_API_KEY` を `.env.local` と Vercel へ
- **完了条件**: オーナーが本番でボタンを押し、引用チップ付きの答えが返り、費用が記録され、正本化後に同じ問いで正本が先に出る
- **最初の一言**: 「実行リストの部品5を superpowers:brainstorming で設計してください。設計書の部品5・仕組み台帳・深掘りの 16・18・20 を入力にし、claude-api スキルでモデルと価格を確認すること」

## 部品6 今日の1問の撤去＋週1の便り

- **目的**: 内の輪を外から引く便りを置き、今日の1問を消す
- **着手条件**: 部品1〜3をオーナーが一周した後
- **作るもの**: 撤去は Recall 設計書「今日の1問の撤去範囲」の表のとおり。週1の便り（Web Push と Resend。メールを止めてもアプリ内の棚に残る）。中身は「今週確かめる主張が n 件」＋傾向1行＋偶然の1行（利用者 ID と週のハッシュで決定的に選ぶ）。0 件なら送らない。曜日の設定（当直の曜日を避ける）
- **傾向の計算**: `recall_review_log`・`recall_progress`・`recall_section_reads`・`recall_claims` の SQL だけ。出してよいもの: 区画ごとの「まだ」率、残した主張の区画の偏り、読んだが残していない節、棚にあって触れていない区画、「まだ」が続く主張の一覧。出してはいけないもの: 点数・順位・赤・責める文言。「得点や順位は能力を認定しない」の断り
- **ここで判断**: クイズタブの端末記録（`quiz-srs.ts`）を Recall へ移すか捨てるか
- **守る継ぎ目**: 13
- **完了条件**: オーナーが本番で便りを1通受け取り、今日の1問が画面と設定から消えている
- **最初の一言**: 「実行リストの部品6を superpowers:writing-plans で計画し、実装してください。撤去範囲は Recall 設計書の表、便りの中身は設計書の部品6の要点のとおり」

## 部品7 記載フォーム＋昇格描画（設計セッション → 実装）

- **目的**: 利用者が MediNode に適した書き方を覚えなくても、Notion にもアプリにも綺麗に出る
- **着手条件**: 部品1の後。/admin の個人・部署リーダーの計測（ブロック分布・離脱タップ）を見てから
- **設計セッションで決めること**
  1. フォームの項目。必須は主張と出典だけ。分野は自動候補（本文を埋め込み、ラベル付きの主張 700 件で最近傍の多数決）
  2. 取り込み経路。最初の追加経路は共有シート。URL（OGP で題名・著者・日付を取り、AI は要約だけ）、スクショ（文字起こし）、音声（転写→整形、整形なしも選べる）
  3. AI 補助の線引き: 形を整えるだけ。補助で入った行は ❓ 既定＋AI 由来の記号。✅ にできるのは出典を伴う行だけ
  4. 規約ブロックの組み立て（Tally の「回答を本文の穴に差し込む」型）。`pages.create` に properties＋children
  5. v1 は新規作成のみ。画像の Notion 保存はファイルアップロード API の版を確認（未確認）
  6. 昇格描画の判定（規約に沿うページだけスプレッドと同じ見え方）と、既存の降格式リーダーとの切替
  7. 複製テンプレ（`notionDuplicatedTemplateId`）の更新。教育はしない
  8. CQ Catcher との線引き
  9. 費用（文字起こし・要約）
- **守る継ぎ目**: 12
- **完了条件**: オーナーが本番でフォームから1ページ作り、自分の Notion に規約どおりのブロックで入り、アプリで昇格描画される
- **最初の一言**: 「実行リストの部品7を superpowers:brainstorming で設計してください。設計書の部品7・継ぎ目12・仕組み台帳・深掘りの 1・3・5・6 を入力にし、2026-08-12 の判断（教育しない・要点モードは個人に出さない）との整合を最初に確認すること」

## 部品8 改訂の旗（設計セッション → 実装）

- **目的**: 主張が改訂・撤回されたとき、残していた人に届く
- **着手条件**: 部品3の後。実際に改訂が1件起きたときを目安
- **設計セッションで決めること**
  1. 旧 ID と新 ID の対応の取り方（同じページ・同じ節・近い本文。埋め込みが使えるなら類似度）
  2. 残していた人への旗の見せ方（球の色か印か）と、カードの差し替え。SRS の記録（段・間隔）は保つ
  3. 出典台帳側の口: **Crossref の Retraction Watch データ**で `doi:` の出典を週1で照合し、当たった主張に旗
  4. 「訂正」の依頼（部品3）が正本化されたときの流れ
  5. 文言: 「既にお使いの方はそのまま。主張だけ新しくなります」の型
- **守る継ぎ目**: 1・2
- **完了条件**: オーナーが本番で主張を1件改訂し、同期後に残していた自分の球に旗が立ち、カードが新しい主張に替わり、段が保たれている
- **最初の一言**: 「実行リストの部品8を superpowers:brainstorming で設計してください。設計書の部品8・深掘りの 17 を入力にすること」

## V 見た目の比較（部品3の後）

- **目的**: 見た目の方向（現行維持か Node Field 寄せか）を、実物を並べて決める
- **やること**: brushup スキルで全画面の棚卸し（読み取り専用・候補7件・1件ずつ承認）。同じ3画面（ホーム・読む画面の主張行・Recall）を現行案と Node Field 寄せ案（紺×白×群青・角丸なし・Archivo／Zen Kaku Gothic New／IBM Plex Mono・デュオトーン）で並べた設計キャンバス（`design` スキル）。常盤色は両案で残す
- **前倒し条件**: 一周の実測で操作に迷った箇所が3つ以上、または drnode.com の公開判断が先に来たとき
- **注意**: 2026-08-27 に「MediNode と Node Field は別の顔」と決めている。寄せる案を採るならその判断を開け直す
- **最初の一言**: 「実行リストの V を進めてください。まず brushup で棚卸し、次に design スキルで3画面×2案のキャンバスを作り、決めるのはオーナー」

---

## オーナーの作業（Claude が代行しないもの）

| いつ | 何を |
|---|---|
| 今 | 管理画面で「サブスク同期」を1回押す（Recall）。残す→確かめる→覚えた の一周を本番で通す |
| 部品2 | push の承認。デプロイ後の手動実行 |
| 部品3 | Supabase で pgvector（と PGroonga）を有効化。埋め込み API キーを `.env.local` と Vercel へ。`ASK_SHELF_EMAILS`。マイグレーションを流す |
| 部品5 | `ANTHROPIC_API_KEY` を `.env.local` と Vercel へ |
| R | Monoxer の特許 JP7495135B2 の請求項を読んだ結果を受けて、伏せ字の段階化を採るか決める |
| 部品7 | /admin の個人・部署リーダーの計測を見る（migration 0025 と先行体験 ON が前提） |
| V | 3画面×2案を見比べて、見た目の方向を決める |

## 決めていないこと（各部品の設計セッションへ）

段0の検索方式と閾値／正本主張ID の書き手／依頼画面の文言／Notion DB の作成の流れと API 世代／段1のモデル・費用・保存先／クイズタブの端末記録／フォームの項目と AI 補助の範囲／昇格の判定／改訂の対応の取り方／見た目の方向。
保留のまま残っているオーナー判断: 「同じ日に同じ主張を確かめた人数」を便りに出すか／ホーム画面ウィジェット（PWA の制約が未確認）／部品6を公開判断と同時にするか。
