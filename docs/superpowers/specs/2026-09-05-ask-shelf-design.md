# 聞ける棚 段0＋段2 設計（部品3）

日付: 2026-09-05
状態: オーナー承認済み（会話で段階確認）。実装計画 `docs/superpowers/plans/2026-09-05-ask-shelf-plan.md`（17タスク）まで完了
正の先行資料:
- Notion「提案005 比較｜検索ゼロ件からCQ・記事化への循環（2026-09-05）」の更新案A〜Hと裁定1〜9
- `docs/superpowers/specs/2026-09-03-cycle-skeleton-design.md`（骨組み・継ぎ目）
- `docs/superpowers/specs/2026-09-02-outer-wheel-design.md`（外の輪）
- 実行リスト `docs/superpowers/plans/2026-09-03-cycle-skeleton-sessions.md` の部品3

## 着手条件の確認（2026-09-05 実測）

| 条件 | 結果 |
|---|---|
| Recall セッションが同期側（主張の抽出・穴・承認）を触り終えている | 満たす。`sync-claims.ts`・`holes.ts`・`/api/admin/recall/cards` の最終変更は 2026-09-02、`extract-claims.ts` は 2026-09-04。いずれも main に入り push 済み。作業中の worktree は無し |
| migration 0024（`user_settings.occupation`） | **本番に適用済み**。`/rest/v1/user_settings?select=occupation` が 200 を返す。`supabase/migrations/README.md` の「❓ 未確認 ※3」は「✅」に更新する |
| 本番の `recall_claims` | 687 行 / 27 ページ。`cloze_status` の承認は 0 |
| 部品2（回答通知） | commit `5f2870e` で main に入り push 済み。`stageOf` は `=== '対応済み'` に直っている。本番での手動実行だけが残る |

## 背景

外の輪は「疑問 → 段0（棚を引く）→ 段2（作者に依頼）→ 正本 → 通知」で閉じる。
現状は3か所が開いている。棚を引く段が無い／終わりが利用者に見えない／正本と依頼が結ばれていない。
本設計はこの3か所を閉じる。段1（AI に組ませる）は部品5で扱い、本設計には含めない。

## 決定事項

### 1. 段0は3層で返す（裁定2・更新案A）

上から「主張」→「節・記事」→「板に出ている近い疑問」。各層は独立して空になりうる。
3層とも空のときだけ、依頼だけが残る。

#### 層1 主張（`recall_claims`）

**方式: 文字2つずつの照合＋覆い率。埋め込みは使わない。**

- 主張の倉庫は増やさない（継ぎ目7a）。`recall_claims` に列を足す。
  - `keywords text not null default ''` … 同期のときにページのキーワード欄を写す
  - `search_text`（本文＋節見出し＋`keywords` の生成列）と PGroonga の索引は、
    **0030 とは別のファイル（0031、任意適用）にまとめて置く**。拡張が入っていない環境では
    `create index using pgroonga` 自体が失敗して 0030 ごと流れなくなるうえ、
    索引を入れない人には誰も読まない列が残るため。0030 だけ流せば段0は動く。
  - **PGroonga が有効でなければ全件を読んで同じ計算をする**フォールバックを持つ。
    687 行では体感差が無いため、Supabase の拡張の有効化を本番投入の前提条件にしない。
- 照合の正は **TypeScript の純関数**（`src/lib/ask-shelf/coverage.ts`）。
  文字 bigram に割り、コーパス全体の出現頻度から重みを付け（珍しい語ほど重く）、
  コーパスに1度も出ない語は最大の重みで扱う。
  **覆い率 = 問いの語の重みの合計のうち、その主張が持っている語の重みの割合。**
  PGroonga は候補を絞るためだけに使い、順位と足切りは常にこの純関数が決める。
  こうすることで、設計時の実測がそのまま本番の振る舞いになる。
- **足切りは覆い率 0.25。** 上位5件まで。
- 並びは覆い率の降順。ただし利用者自身が残した主張（`recall_progress` に有効な行がある）を
  最上位に置き、「あなたが残した」の印を付ける（継ぎ目7b）。
- 0件のときの1行は「**MediNodeにはこの問いの検証済みの主張はまだありません**」（更新案B）。
- 各主張に「残す」と「この節を読む」を置く（継ぎ目7c。主張を行き止まりにしない）。
  飛び先は読む画面の該当節（`section_key` の `sec{n}`）。

##### 実測（2026-09-05・本番 `recall_claims` 687 行）

問い38件を通した。棚にある側27件はサブスク記事の題名（＝臨床の疑問文）で、正解はそのページ由来の主張。
棚に無い側11件は、公開中の未解決の板に出ている5件と、コーパスに無い分野6件（小児・産科・制度・眼科・整形・精神）。

| 物差し | 正解が1位 | 上位5位 | 棚に無い問いを誤って出す |
|---|---|---|---|
| 点数（BM25）だけ | 81% | 96% | **切り分け不能**。棚に無い問いの最高点 36.6 が、棚にある問いの最低点 16.8 を上回る |
| 覆い率（本文＋節見出し＋キーワード） | 96% | 100% | **0 / 11** |

覆い率の分布は、棚にある側が中央値 0.40・最小 0.19、棚に無い側が中央値 0.15・最大 0.19。

| 閾値 | 棚にあるのに「無い」と言う | 棚に無いのに出す |
|---|---|---|
| 0.25 | 2 / 27 | 0 / 11 |
| 0.30 | 6 / 27 | 0 / 11 |
| 0.40 | 13 / 27 | 0 / 11 |

0.25 を採る。点数で切れない具体例として、「尿道カテーテルはいつ抜くべきか」（棚に無い）が
乏尿の記事の主張に 36.6 点を付ける。覆い率では 0.14 で、正しく「無い」になる。

**この実測の限界。** 棚にある側の問いは記事の題名なので、実際の利用者の言い回しより有利である。
本番では「無い」が実測より増える見込み。だから層2・層3が要る。主張の層は厳しく断ってよい。
閾値は定数として1か所に置き、`ask_shelf_queries` の記録を見て後から引き直せるようにする。

**埋め込みを採らない理由。** 上の精度が API キー・pgvector・検索ごとの費用なしで出ており、
かつ埋め込みでも「無い」と言う閾値は別に要る（コサイン類似度は水準がそろわない）。
再検討ライン: 主張が 2,000 を超えたとき、または覆い率 0.25 の取りこぼしが記録上 3割を超えたとき、
埋め込みを日本語の医学文で実測して比べ直す。

#### 層2 節・記事（既存の検索索引）

- 既存のサブスク索引に `filters: 'recordType:section'` で問い合わせ、上位3件。新しい索引は作らない。
- 節レコードは `src/lib/subscription-sections.ts` が作っており、`objectID` は `<親ID>#sec{n}`。
  主張の `section_key` と同じ規則なので、同じ節を指せる。
- 層1で既に出した節は層2から落とす（同じ場所を二度出さない）。
- 表示は 記事の題名 → 節見出し →「この節を読む」。

#### 層3 板の近い疑問

- `/api/cq/board` が返す5件を、層1と同じ覆い率で並べ替え、0.15 以上の上位2件。
  **0.15 は実測していない出発点**（層1の 0.25 より緩めに置いた値）。母数が5件しかなく、
  誤って出しても「近い疑問」として読まれるため、層1より緩くてよいという判断。
  層1と同じく定数として1か所に置き、`ask_shelf_queries` の記録を見て引き直す。
- 「私も気になる」は既存の投票 API をそのまま使う。上限（1日60回）も変えない。

#### 無料と有料（継ぎ目9）

- 主張の本文は有料。無料の利用者に返してよいのは、題名・節名・件数と「棚に無い」の1行まで。
- オーナー専用の期間は判定が要らないが、**返すデータを組み立てる段階で最初から通す**。
  後から足すと本文が漏れる。

#### 計測（更新案H）

`ask_shelf_queries` を1つ作る。列は 問い／層ごとの件数／最高覆い率／依頼に進んだか／作成時刻／利用者。
ここから「段0を見せた後に送らずに済んだ割合」を /admin に出す。

### 2. 利用者に見える状態は4つ（裁定3・更新案C）

判定は既存の `cq-mine.ts` の `stageOf` をそのまま使う。4段（received / onBoard / answered / closed）を既に返している。
足りないのは表示と理由だけ。

| 受付DB | stage | 見える文言 |
|---|---|---|
| 対応状態が空・ボード公開OFF | received | 受け付けました |
| 対応状態が空・ボード公開ON | onBoard | 検討中（板に出ています） |
| 対応済み | answered | 公開されました（飛び先つき） |
| 対応不要 | closed | 今回は記事化しません（＋理由） |

`src/lib/cq-dispatch.ts` の `dispatchLabel` に closed の分岐を足す。

**理由の置き場所は受付DBの新しい select 列「見送りの理由」。** 選択肢は5つ。

1. MediNode の対象外
2. 個別の症例の判断による
3. 既存の記事で答えられる
4. 根拠を確認できない
5. いまの制作範囲では扱えない

3つ目だけは「対応不要」ではなく **「対応済み」＋「正本主張ID（既存の主張）」と組ませる**。
利用者は答えが手に入るので、「記事化しません」ではなく「公開されています、ここです」が正しいため。
残る4つが「対応不要」と組んで「今回は記事化しません」になる。

これにより **受付DBに足す列は4つ**（継ぎ目4を3つから4つへ改める）。
段0結果（select: 該当あり／該当なし／未実行）／段0主張ID（rich_text）／正本主張ID（rich_text）／見送りの理由（select）。
既存の列は変えない。列が無い受付DBでも既存動線が壊れないこと（`buildIntakeProperties` は無い列を積まない）。

### 3. 正本主張IDの書き手は管理画面

/admin に「聞ける棚」パネルを1つ置く。

- 未対応の依頼の一覧。**段0結果＝該当なし で絞ったものが「空白候補ビュー」**（独立した保管場所は作らない）
- 各行で、ボード公開の切替／対応状態／見送りの理由／正本の主張の選択
- 主張の選択は語で検索して選ぶ。**24文字の鍵を手で写させない**（写し間違いは誰にも気づかれず、通知が飛ばないだけになる）
- 書き込み先は Notion の受付DB。Supabase に別の真実を作らない
  （2026-09-06 追記・提案010 判断1: この原則は保つ。ただし投稿の入口だけは順序を逆にし、
  `cq_submissions` に全文を先に残してから Notion へ書く。Notion が落ちた分は待避行として残り、後から再送する。§9）

**制作工程との受け渡し（`medinode-cq-note` の中身は変えない）**

1. 入口: /admin の空白候補から疑問文と背景をコピーしてスキルに渡す
2. 出口: 記事を公開 → サブスク同期で `recall_claims` に主張が入る → /admin で正本の主張を選ぶ
   → 「対応状態＝対応済み」と「正本主張ID」が Notion に書かれる
3. 通知 cron が次の実行で拾う

### 4. 回答通知（裁定4・更新案D/E、継ぎ目5の書き換え）

**飛び先は具体的な順に決める。** 純関数 `answerLandingUrl` に切り出す。

1. 正本主張ID があり、その主張が `recall_claims` にある → 着地画面 `/cq/answered/[id]`（節まで分かる）
2. 主張は分からないが記事は分かる → 着地画面から記事の先頭へ
3. 何も無い → アプリの入口（従来の振る舞い）

**「主張は無いが節は分かる」という段は置かない。** 節が分かるのは主張が `recall_claims` に
見つかったときだけなので、到達できない分岐になる（使われない分岐を残さない）。

URL は着地画面1つにまとめる。アプリには「記事の特定の節を URL で開く」経路が無く
（リーダーは `objectID` でアプリ内から開く。`src/lib/vine-open.ts`）、ここで URL の形を
新しく作ると開き方が二重の規約になるため。「主張＞節＞記事」の順位は、
着地画面が何を見せるかとして表す。

**着地画面 `/cq/answered/[id]`**（本人だけが開ける）に4つを並べる。
自分が送った疑問／回答＝正本の主張（本文と確信度）／根拠＝出典／「残す」と「この節を読む」。

**プッシュ**は cron の中で `sendToUsers(admin, [userId], 'resolved_cq', { title, body, url })` を1回。
種別 `resolved_cq`・設定トグル・`sw.js` のクリック先（payload の `url`）はすべて既にある。呼ぶ側だけを足す。
重複防止はメールと同じ `user_metadata.cq_answer_notified` を使い、1件につき1回。

**継ぎ目5の残り（2026-09-05 提案006の裁定1で書き換え）**。正本化の合図は「対応済み」かつ「正本主張ID が空でない」。
**cron は依頼者の `recall_progress` に書かない。** 「残した」になるのは、依頼者が着地画面の「残す」を押したときだけで、
そのときは既存の `/api/recall/keep` が通常どおり「間隔1日・期限翌日」で始める。
理由: 本人が押していない主張を出題母集団に入れない（継ぎ目10「一括操作を置かない」と同じ根拠）。
これにより「`due_at` を null にして初回閲覧から始める」という表現は不要になった
（`due_at` は `not null` で、離れかけの判定 `remainingOf()` は `due_at` を見ずに `last_reviewed_at` から計算するため、
null 方式はスキーマと計算の両方に合わなかった）。
フラグが閉じている依頼者にも着地画面は開けるが、「残す」は `recall` の内側でだけ描く。メール文面に Recall の名前を出さない。

### 5. 依頼画面の文言（裁定5・更新案F）

ボタンと見出しを「**MediNodeに足してほしい疑問**」に変える。
送信ボタンの上に、畳まずに5点を出す。

1. 個別の患者さんへの診療の助言はできません
2. 急いでいる判断には間に合いません
3. 患者さんが特定できることは書かないでください
4. すべてが記事になるわけではありません
5. 記事になったら公開されます。いつまでに、の約束はできません

**同時に変える場所**（1つでも残すと言葉が割れる）

- 6つの入口のボタン（検索ゼロ件／右下の丸／記事内の丸／設定／未解決の問い画面のパネル／同画面の空のとき）
- 完了画面（「専門医に届きました」を差し替える）
- 回答通知メールの件名と本文
- 外部 Notion フォームの説明文（文案を出し、Notion 側はオーナーが直す）

背景欄の例文「患者背景・場面・これまでの対応など」は、注意3と矛盾しない書き方に直す。

### 6. 小さな修正3つ（裁定6・7・8）

3つとも同じ投稿の経路を触るので、部品3に同梱する。分けると同じファイルを二度触って衝突する。

- **月5件の上限**: 受付DBの自分の行のうち直近30日を数える。Upstash は本番に設定された記録が無く、
  未設定だとメモリ版に落ちて30日を覚えられないため使わない。1日5件と1IP20件は従来どおり残す。
  案内は上限に近づいたときだけ出す（残り1件で「今月あと1件です」）。
  通知に同意していない投稿は数に入らない（同意の線引きを機能のために広げない。既存方針）。
- **掲載名の希望**: `buildIntakeProperties` に列を1つ足し、フォームに「板に出すときの名前」（匿名／ペンネーム）を置く。
  `cq-board.ts` は既にこの列を読んでおり、未選択なら匿名（安全側）のまま。
- **経験年数・診療科をアカウントに保存**: `user_settings` に列を2つ足し、`account-profile.ts` を広げる。
  /admin の属性別集計は実装の最後の段に置き、保存だけ先に本番へ出せるようにする。

### 7. 今回は入れないもの（理由つき）

| 入れないもの | 理由 | 送り先 |
|---|---|---|
| 音声入力 | 部品7の記載フォームと同じ仕組みになる。片方だけ作ると実装が2つに割れる | 部品7 |
| 「この主張は違う」の訂正 | 受け皿の旗が部品8。返らない入口を増やすことになり、いま直している問題そのものになる | 部品8 |
| 期待値の「平均○日・正本化率○%」 | 出所を書ける数がまだ無い。注意5点の4番でまかなう。3か月ぶん記録がたまってから入れる | 実測後 |
| 依頼フォームの必須項目を増やす | 投稿がさらに減る。注意5点を足すだけにする | ― |
| 検証者名・検証日の表示 | `recall_claims` に列が無い。同期に足す話なので `revised_at` と一緒に扱う | 部品8 |
| 「作成中」「統合」の状態、代表CQへの統合 | 裁定で採らないと決定済み | 提案006 |

ヘルプに「棚に無ければ答えない」を仕様として1節書くのは入れる（設定画面のヘルプ）。

### 8. 提案006「Nodeの情報構造」の裁定（2026-09-05）

比較の正は Notion「提案006 比較｜Nodeの情報構造（Claim＋利用者状態）（2026-09-05）」。
結論は「Node＝主張＋利用者状態」は現行の実装そのもので、新しい表は要らない。設計書に足すのは次の3つ。

**用語と実体の対応表**

| 語 | 意味 | 実体 |
|---|---|---|
| 主張 | 全利用者に共通する検証済みの1行 | `recall_claims` 1行。同期だけが書く |
| Node | その主張と自分の状態を合わせた、画面上の点（読む画面の行末の丸・Recall の点） | 描画時に `stateOf()` で合成。保存しない |
| 残した／読んだ／確かめた | 利用者ごとの状態 | `recall_progress`／`recall_section_reads`／`recall_review_log`。本人だけが書く |
| 定着 | 間隔90日以上の表示上の呼び名 | 計算値。列ではない |

UI 文言は「主張」のまま。コードの改名はしない（裁定6）。

**情報ごとの管理主体**

| 情報 | 管理主体 |
|---|---|
| 記事・節・主張の本文、確信度マーク、参考文献 | 作者（Notion） |
| 主張ID、節キー、`recall_claims` の行 | サブスク同期（`_core.ts`）だけ |
| 読んだ | 利用者の明示操作（節末ボタン） |
| 残した | **利用者の明示操作だけ**（読む画面の Node・Recall のカード・段0・着地画面の「残す」。すべて `/api/recall/keep`）。cron は書かない |
| 確かめた・間隔・期限 | アプリ（`srs.ts`）。利用者は「覚えた／まだ」を選ぶだけ |
| 依頼の原文・投稿時点の職種・経験年数・診療科 | 利用者の投稿（受付DBと `cq_submissions` に複製。後から変えない） |
| 対応状態・ボード公開・見送りの理由・正本主張ID | 作者（/admin から。Notion 手入力はしない） |
| 明るさ・離れかけ・次の期限・依頼の4状態 | アプリの計算 |

**裁定**

1. 正本化で cron が「残した」を書かない（上記4に反映）
2. `due_at` null の表現は不要になった
3. 「正本主張ID」は **/admin では1件だけ選ぶ**。読む側（`readIntakeColumns`）だけ配列を返す形にしておき、
   通知と着地画面は先頭の1件を使う。再検討ライン: 1つの依頼に2節以上で答えた実例が1件出たとき
4. 「記事だけを回答先にする」列は足さない。回答先は必ず主張1つ。実例が出たら足す
5. `/api/recall/keep` の `keep:true` に `recall_claims` の存在確認を足す（部品3で呼び手が増えるため入口で締める）
6. 用語「Node」を設計書の語として採る（上の対応表）

採らないもの: 代表CQ・CQ↔回答先の多対多・主張↔文献の中間表・概念ID。
再検討ライン: 読者由来の依頼が月10件を超えたとき、または同じ主張に3件以上の依頼が集まったとき。

### 9. 提案010「Notionとの関係」の裁定（2026-09-06）

比較の正は Notion「提案010 比較｜Notionとの関係（3種類のNotion・管理主体・同期方向）（2026-09-05）」。
結論は「Notion＝作る場所、MediNode＝検索・閲覧・Node・Recall・CQ・通知を実行する場所」で、**現行がすでにこの形**。
同期方向の表は骨組み設計書の継ぎ目15の隣に置いた（判断5）。オーナー裁定は7件。

| # | 判断 | 裁定 | 影響 |
|---|---|---|---|
| 1 | 作者へ送る CQ の正本 | **C** 受付DBのまま。投稿時に `cq_submissions` へ全文を先に残し、Notion 失敗時は待避行から再送 | `submit/route.ts` の順序と migration 1本。読む側（`cq-mine`・板・通知 cron・月5件・/admin）は触らない |
| 2 | サブスク原本の公開共有（`notion.site`） | **A** 切る方向。配った先の棚卸しはオーナーの運用作業 | コードでは閉じられない。切る前に原本1ページで同期が通ることを実測する |
| 3 | 自分用 CQ を MediNode に保存 | **採る**。表の設計は部品4の「決めること7」に寄せる | 部品4の入力。ここでは表を作らない |
| 4 | 個人 Notion の検索DBと書き込みDBの分離 | **分けない** | 変更なし。分けるのは部品4の落とし先（新設DB）だけ |
| 5 | 同期方向の表を設計書へ | **置く** | 骨組み設計書に反映済み。コード変更なし |
| 6 | 自動書き出しのタイミングと復号の範囲 | **部品4で決める** | 実行リストの部品4 決めること3に注記済み |
| 7 | 索引の `notionUrl` と表示の例外2つ | **B** サブスクのレコードから落とし、例外2つを導線なし／リーダー起動に揃える | 判断2とセット。索引の再構築が1回要る |

**判断1の実装メモ（着手する人向け）**

- 現行は `notion.pages.create` → `logCqSubmission` の順で、Notion が落ちると catch に入り 500。`cq_submissions` にも残らない
- `cq_submissions.question` はいま先頭200字。待避行から再送するなら全文が要る。列の追加（全文・待避の印・再送日時）は migration で
- 順序を変えても月5件の数え方（受付DBを数える `listAllIntakePages`）は変えない。待避行は「まだ Notion に無い投稿」なので、
  数える対象に含めるかどうかを実装時に決める（含めないと、Notion 障害中に上限を超えて投稿できる）

**判断7の実装メモ**

- 例外は2つ。今日の1問の「監修ページで続きを読む」（`DailyQuestionCard.tsx`）と、最近見たの外部リンク（`RecentViews.tsx`）。
  どちらも `hasSubscriptionConfig()` が false のとき＝購読が切れた人にだけ出る。判断2を実行すると**開けないリンクになる**
- 降格リーダーの「Notionの方が読みやすそうです」は `isPersonalDoc` で絞られており、作者ページには出ない（2026-09-06 確認）
- リーダー右下の CQ ボタンが出典として `hit.notionUrl` を渡している（`ReaderOverlay.tsx`）。落とすなら記事タイトルか objectID に差し替える
- `vine-open.ts` の `leafDestination` は未購読のサブスクに `{mode:'none'}` を返しており、この方針と既に一致している

**採らないもの**: 作者へ送る CQ の MediNode 正本化（判断1 B）・個人 Notion の検索DBと書き込みDBの分離。
再検討ライン: Notion 側の失敗で投稿が落ちた実例が1件出たとき、または読者由来の依頼が月10件を超えたとき。

## 作るもの

**migration 0030**

- `recall_claims` に `keywords`
- `ask_shelf_queries`（RLS 有効・ポリシー無し。service_role のみ）
- `user_settings` に `experience_years`・`doctor_departments`

**migration 0031（任意適用）**

- PGroonga の拡張・`search_text`（生成列）・索引。無くても段0は動く

**Notion 受付DB** — 列4つを手で足す（オーナー作業）

**コード**

| 置き場 | 中身 |
|---|---|
| `src/lib/ask-shelf/coverage.ts` | 覆い率の純関数と閾値の定数 |
| `src/lib/ask-shelf/rank.ts` | 3層の組み立て・重複落とし・無料有料の絞り |
| `src/app/api/ask-shelf/search/route.ts` | `hasFeature('ask_shelf')` の内側 |
| 検索タブ | 主張の段（あるときだけ開く・折りたたみ可）。窓とタブ構成は変えない（継ぎ目8） |
| `src/lib/cq-submit.ts` | 掲載名の希望・月5件・注意5点に伴う文言 |
| `src/lib/cq-dispatch.ts` | closed の表示と理由 |
| `src/lib/cq-answer-notify.ts` | 飛び先・プッシュ（`recall_progress` には書かない。2026-09-05 裁定1） |
| `src/app/api/recall/keep/route.ts` | `keep:true` に `recall_claims` の存在確認を足す（無い・inactive は 404。2026-09-05 裁定5） |
| `src/app/cq/answered/[id]/` | 着地画面 |
| `src/app/admin/` | 聞ける棚パネル（空白候補・正本の主張を結ぶ） |
| `src/lib/feature-access.ts` | `ask_shelf` を足す |
| `src/lib/recall/sync-claims.ts` | `keywords` を写す |

## 守る継ぎ目

1（主張の鍵は `claimIdOf` 1つ）／2（残したの記録は `recall_progress` 1本）／
4（**3つ→4つに改める**。4つ目は「見送りの理由」。既存の列は変えない・無い列は積まない）／5（上記4のとおり書き換え。**cron は `recall_progress` に書かない**）／
6（`ask_shelf` フラグ・フォールバック無し・判定はサーバーが正）／7（上記1のとおり書き換え。(a)(b)(c) は保つ）／
8（窓とタブ構成は変えない。投げ込みは結果の末尾へ降格）／9（無料と有料の線）／10（一括操作を置かない）

## 完了条件

オーナーが本番で次を通したときに閉じる。

1. 疑問を書いて段0が主張を返す／「MediNodeにはこの問いの検証済みの主張はまだありません」を出す
2. 依頼を送る（注意5点が見えている・掲載名の希望が板に反映される）
3. 制作工程で正本化し、/admin で正本の主張を結ぶ
4. サブスク同期のあと、メールとプッシュが届き、着地画面から該当節へ飛べる
5. 着地画面で「残す」を押すと球に「残した」で灯り、期限は翌日になる（押さなければ灯らない）
6. 「対応不要＋理由」にした依頼が、依頼者の画面で「今回は記事化しません」＋理由になる

**数で見る2つ**（更新案H）

- 段0を見せた後に送らずに済んだ割合（`ask_shelf_queries` の `submitted=false` の割合）
- 「記事化しない」を見た依頼者が30日以内に再投稿した件数

## オーナーの作業

| いつ | 何を |
|---|---|
| 実装の前 | 受付DBに列4つを足す（段0結果／段0主張ID／正本主張ID／見送りの理由） |
| 実装の後 | migration 0030 を Supabase の SQL Editor で流し、README の表に印を付ける |
| 実装の後 | `ASK_SHELF_EMAILS` に自分のメールを入れる（`.env.local` と Vercel） |
| 任意 | Supabase で PGroonga を有効にし、migration 0031 を流す（無くても動く。速さのためだけ） |
| 公開判断のとき | 無料と有料の判定を有効にする・`ASK_SHELF_GA` |

`supabase/migrations/README.md` の 0024 の行を「✅（2026-09-05 実測）」に直すことを、実装の最初の段に含める。

## テストの通し方

- 覆い率の純関数は、本番から落とした 687 主張と、この設計で使った問い38件を
  **2026-09-05 時点の写しとして `.preview/` に置き**、回帰にする。自作の文字列同士では比べない。
  **リポジトリにはコミットしない。** 主張の本文は有料のサブスク本文であり、ここは公開リポジトリである
  （`.preview/` は `.gitignore` 済み）。写しが無い端末ではこの回帰はスキップする。
  板の5件は運用で入れ替わるので、写した時点を添えて固定する（後日ずれても、それは板が変わっただけ）
- 段0が該当なしのときに「棚に無い」の1行が必ず出ることを回帰で固定する（外の輪の設計から引き継ぎ）
- 飛び先の純関数は、3つの分岐すべてに実データ形の入力を通す
- 通知 cron は本番で `CRON_SECRET` 付きに1回手動実行して件数を見る

## 本設計で決めないこと

段1のモデル・費用・保存先（部品5）／Notion への落とし（部品4）／改訂の旗と訂正の受け皿（部品8）／
代表CQへの統合と「気になる」を押した人への通知（提案006）／保存先の広げ方（提案010）／見た目の方向（V）
