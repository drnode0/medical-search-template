# 双輪サイクルの骨組み（部品の並び・継ぎ目・回し方）

日付: 2026-09-03
状態: オーナー承認済み（会話で段階確認。案「1周を先に通す」を採用）。各部品の実装計画は部品ごとに別セッションで起こす
先行資料:
- 外の輪 `docs/superpowers/specs/2026-09-02-outer-wheel-design.md`（段0／段1／段2の契約）
- 内の輪 `docs/superpowers/specs/2026-09-02-recall-engine-design.md`（主張・球・確かめる・SRS・テーブル・今日の1問の撤去範囲）
- 双輪の骨格（2026-09-01。正は Notion 🎬プロジェクト_DB「MediNode アプリ開発」配下）

## 背景・課題

内の輪（Recall）は実装が本番に入り、外の輪は設計が承認された。しかし両輪を「一周」として回した人はまだいない。
コードの現物を辿ると、輪は次の箇所で途切れている（2026-09-03 に読み取り専用で確認）。

外の輪（疑問 → 正本）
- 疑問の入口が3つに割れ、返るものが違う。検索はページ着地で終わり、投げ込みは「届きました」で待ち、
  節末の「気になる」は票数だけを返す。段0（棚を引く）は未実装
- 作者側の操作（「対応済み」への変更・ボード公開・「気になる」の集計の目視）はすべて Notion 手作業で、
  アプリは受付DBを読むだけ。書き戻す API が無い
- 正本になった事実がアプリへ戻る経路は、未コミットの通知 cron（`cq-answer-notify`）だけ。
  「依頼者には最初から残した状態で入る」接続点は未実装で、受付DBの行と `recall_claims` を結ぶ列が無い
- 送った疑問の追跡がサーバー側（`cq-mine`）と端末側（`cq-dispatch`）の二重

内の輪（読む → 残す → 確かめる → 定着）
- 読む画面に「残す」が無い。確信度マーク（`ConfidenceMark`）はアイコンだけで押せず、「残す」は球の閲覧カードに閉じている
- 節の読了 API（`/api/recall/read`）に呼び手が無い。「読んだ」の光は一度も点かない
- 残した主張は利用者の Notion に落ちない（設計未着手）。ただし利用者のトークンで DB を新規作成する
  API（`/api/notion/create-db`）が既にあり、OAuth の権限も読み取り＋挿入まで持つ
- クイズタブ（端末保存・赤マーカー母集団）と今日の1問が Recall と並存している

制作工程との接点
- 段2の依頼は受付DBに入るが、正本化した主張を依頼に結ぶ列が無い

本設計は、これらを閉じる部品の並びと、どの部品も勝手に決めてはいけない継ぎ目を固定する。
部品の中身の設計は、必要な部品だけ別セッションで行う。

## 用語

- **主張／残す／読んだ／確かめる／定着**: Recall 設計の定義に従う
- **段0／段1／段2**: 外の輪の設計に従う（棚を引く／AI に組ませる／作者に検証を依頼）
- **正本化**: 依頼された問いが制作工程を経て公開ページの主張になること
- **部品**: 1セッションで設計または実装する単位。本設計では 0〜8 の9個（8 はパーツ調査で追加）
- **継ぎ目**: 複数の部品が同じものを読み書きする箇所。本設計で固定し、部品の設計で変えない

## 目的と成功条件

**目的**: 双輪サイクルを、オーナー1人が本番で一周できる状態にする。すべて先行体験のフラグ（`recall`・`ask_shelf`）の内側で行う。

**成功条件**（オーナーが本番で実測して判定する）

- 内の輪: 記事を読む → 主張行で「残す」→ 球で光る → 翌日「確かめる」→ 覚えた／まだ → 記録が Supabase に残り、
  自分の Notion の DB に主張が1行入る
- 外の輪: 疑問を書く → 段0が主張を返す（無ければ「棚に無い」の1行）→ 依頼 → 受付DBに段0の結果つきで入る →
  オーナーが制作工程で正本化 → サブスク同期で主張が入る → 依頼者にメールが届き、その主張が「残した」状態で球に灯る
- 段1: ボタンで AI 回答が返り、未検証の表示と引用チップが出て、MediNode 側に保存される

**非目標**

- 一般公開（drnode.com の公開判断まで先行体験の内側）
- 見た目の方向の決定（部品3の後に、現行維持と Node Field 寄せを並べて決める。それまでは現行の写しに合わせる）
- クイズタブと Recall の統合（部品6で判断だけ行う）
- 個人・部署ナレッジの主張を球に出すこと（v2）
- 制作スキル（CQ・Essentials・検証層）の中身の変更。本設計が触るのは受付DBの列という接点だけ

## 完成形の体験（オーナー専用の状態で回す1週間）

1. **現場で3秒。** 検索タブの窓に疑問を自然文で書く。上に段0の主張が数件（各行に「残す」と「この節を読む」）、
   下に今までどおりのページ。主張が無ければ「この問いそのものの主張はまだ棚に無い」の1行と近い主張、
   その下に「作者に検証を依頼」と「AI に組ませる（未検証）」
2. **依頼。** 受付DBに「段0結果」つきで入る。受付画面に「票の多い順に、月に数件を正本にします」の1行が出る
3. **閲読。** 主張行の右端の「残す」、節末の「この節を読んだ」。残した行は翌朝、自分の Notion の DB に入っている
4. **確かめる。** 球の「確かめる」で期限の来た主張が最大5枚。伏せ字カードか想起カード。裏は原文と出典
5. **正本が戻る。** 制作工程で正本化 → 同期 → 依頼者にメールとプッシュ → 着地画面に疑問・回答（主張）・根拠・「残す」。
   「残す」を押した主張だけが球に灯り、期限は翌日（2026-09-05 提案006の裁定1で書き換え。旧: 自動で「残した」・期限は初回閲覧から）
6. **週1の便り。** 「今週確かめる主張が n 件」と傾向1行（「まだ」が多い区画、棚にあって自分が触れていない区画）

一般の利用者は、この期間は今日と同じ画面（フラグの外側）。今日の1問だけが部品6で消える。

## 部品と順番

| 順 | 部品 | 作るもの | 設計セッション | 実装 |
|---|---|---|---|---|
| 0 | `DESIGN.md` | 既存の確定デザイン（Noto Sans JP・常盤 #196B4F・lucide）から、色・字の大きさ・余白・角丸・ボタンとカードの型を1枚に起こす | 不要 | Sonnet 5 |
| 1 | 読む画面の「残す」と「読んだ」 | 主張行に「残す」の切替、節末に「この節を読んだ」。既存の Recall API を呼ぶ。`recall` の内側にだけ描く | 不要（本設計で決める） | Sonnet 5 |
| 2 | 回答通知の仕上げ | 実装済みの cron（`cq-answer-notify`）と `cq-mine` の修正をコミットし、本番で1回手動実行して確認 | 不要 | Sonnet 5 |
| 3 | 聞ける棚の段0と段2 | 検索タブに主張の段、依頼に段0の結果を添える受付DBの列、正本化の着地画面（依頼者がそこで「残す」）、`ask_shelf` フラグ | 要 | Opus 5 |
| 4 | Notion への一方向落とし | 残した主張を利用者の Notion に自動作成した DB へ書く。既存の DB 作成 API を土台にする | 要 | Opus 5 |
| 5 | 段1（AI に組ませる） | ボタンで踏む AI 回答、未検証表示、引用チップ、保存。Notion 接続済みなら 4 の経路で落とす | 要 | Opus 5 |
| 6 | 今日の1問の撤去と週1の便り | 撤去範囲は Recall 設計書の表。入れ替えに週1の便り（件数＋傾向1行）。クイズタブの端末記録を Recall へ移すかをここで判断 | 小 | Sonnet 5 |
| 7 | 自分のナレッジの入口と出口（記載フォーム＋昇格描画） | 書く側: アプリ内の記載フォームが、テンプレ規約どおりのブロックを利用者の Notion DB に書く（URL の要約・スクリーンショットの文字起こしなど AI の補助つき）。読む側: テンプレに沿ったページをサブスクのスプレッドと同じ見え方で描く。降格式が既定 | 要 | Opus 5 |
| 8 | 改訂の旗（鮮度） | 主張が改訂・撤回されたとき、残していた人に旗を立て、カードを差し替え、SRS の記録は保つ。出典台帳側で撤回・改訂を拾う口も決める | 要 | Opus 5 |

順番の根拠
- 0 は以後の全部品が見た目の基準にするので最初
- 1 と 2 は土台が既にあり、一周の両端を先に繋ぐ
- 3 は正本の紐づけ（受付DBと主張の対応）を決める部品で、4 より前に置かないと、先に残した記録を付け替えることになる
- 5 は費用の実測が要るので、3 で疑問の流れが通ってから乗せる
- 6 は 1〜3 をオーナーが一周した後。撤去だけを先にすると、内の輪を外から引くものが無い期間ができる
- 7 は読む画面の部品を 1 が先に触るので後。2026-08-12 の判断（要点モードは個人ページに出さない・書き方の教育をしない）を
  一部開け直すため、/admin に貯まる計測（ブロック分布・離脱タップ）を見てから設計する
- 8 は同期側（主張の ID が変わる検出）と出典台帳側（撤回・改訂の検出）の両方に触るので、3 の後。実際に改訂が1件起きたときを目安にする

### 各部品の要点

**部品0 `DESIGN.md`**
- 置き場はリポジトリ直下。中身は現行のトークンの写しであり、新しい見た目を決めない
- 実装セッションはこれを読んでから画面を足す

**部品1 読む画面の「残す」と「読んだ」**
- 対象はサブスクのアプリ内リーダー（`ReaderBody` と `ReaderSpread`）。個人・部署ページには置かない（v1 の主張はサブスク限定）
- 「残す」を置く行は、主張抽出（`extract-claims.ts` の `splitClaim`）が主張と判定する行だけ。判定は共有の純関数を使い、
  行の本文から `claimIdOf(ページID, 本文)` を計算して `/api/recall/claims` の一覧と突き合わせる。
  一覧に無い行（未同期・除外対象）には置かない
- 「この節を読んだ」は節末の明示ボタン。節の番号は主張抽出と同じ規則（番号付き H2 → `sec{n}`）。
  スクロールでの自動判定はしない（Essentials 1本で71件が「読んだ」になるのを避ける）
- 既存の端末ローカルの読了水位（`ReaderMarksProvider`。続きから読むための位置）は別物として残し、統合しない
- 「残す」に一括操作を置かない（後述の継ぎ目）
- `hasFeature('recall')` が偽の利用者には、ボタンも節末の操作も描かない

**部品2 回答通知の仕上げ**
- 実装済み。`vercel.json` の cron（`/api/cron/cq-answer-notify`）と `cq-mine` の「対応済み」判定を含めてコミットし、
  本番で `CRON_SECRET` 付きで1回手動実行して `mailed` の件数を見る
- 部品3で「正本主張ID」列が入るまでは、メールだけを送る（従来の振る舞い）

**部品3 聞ける棚の段0と段2**（設計セッションで決めること）
- 段0の検索方式（埋め込みの種類・日本語全文検索との併用・更新頻度）。列は `recall_claims` に足す
- 「正本主張ID」を誰が書くか（管理画面の操作か、Notion での手入力か）
- 依頼の受付画面の期待値の文言と、「対応不要」になったときの理由の見せ方
- 空白候補ビュー（受付DBの1ビュー。段0結果＝該当なしで絞る）
- **制作工程との受け渡し**: CQ スキル（`medinode-cq-note`）が受付DBの依頼を入口として読む手順と、正本化したときに
  「対応状態＝対応済み」と「正本主張ID」を書き戻す手順。スキルの中身は変えず、入口と出口の2手順だけを決める
- **音声で疑問を入れる**: 検索タブの窓に音声入力を置くか。置くなら、AI が整えるのは疑問文の形だけで、答えは作らせない
- **訂正の種別**: 依頼に「この問いの主張が棚に無い」だけでなく「この主張は違う」を主張ごとに投げられる入口を置く。受付DBの種別列（既存の列で足りるか）と、部品8の改訂の旗への繋ぎを決める

**部品4 Notion への一方向落とし**（設計セッションで決めること）
- DB の自動作成の流れ（既存の `/api/notion/create-db` の流用範囲）と、作成済み DB の ID の保存先（`user_settings` の暗号化設定）
- 書き込みのタイミング（残した直後か、まとめてか）と失敗時の再送
- Notion API の世代（現行の 2022-06-28 固定のままか、2025-09-03 版へ上げるか）。参照実装は SUBSCreen

**部品5 段1**（設計セッションで決めること）
- モデルの選定と費用の記録。表示規約（未検証・引用チップ・由来なし）。保存先のテーブル
- 費用方式の候補: 1日 n 回まで無料にして残り回数を表示する（Pocket-ER の型）。実測後に決める

**部品6 今日の1問の撤去と週1の便り**
- 撤去範囲は Recall 設計書「今日の1問の撤去範囲」の表のとおり
- 週1の便りは既存のプッシュ基盤（`push_subscriptions`・`push_notify_prefs`）で送る。日次にしない。
  中身は「今週確かめる主張が n 件」と傾向1行。傾向は `recall_review_log`・`recall_progress`・`recall_section_reads` と
  `recall_claims` から計算だけで出す（AI の作文は使わない）
- 傾向で出してよいもの: 区画ごとの「まだ」の率、残した主張の区画の偏り、読んだが残していない節、
  棚にあって自分が一度も触れていない区画。出してはいけないもの: 点数・順位・赤い表示・未解決の数を責める文言
- クイズタブの端末記録（`quiz-srs.ts`・localStorage）を Recall へ移すか捨てるかは、ここで判断する

**部品7 自分のナレッジの入口と出口（記載フォーム＋昇格描画）**（設計セッションで決めること）
- 書く側（記載フォーム）: 利用者が MediNode に適した書き方を自分で覚えなくても済むように、アプリ内のフォームに入力すると
  テンプレ規約どおりのブロックが利用者の Notion DB に書かれ、同じ原本がアプリ内にも描かれる。
  他の AI（ChatGPT など）で得た回答の保存先にもなる（流れていく回答を止める入口）
- フォームの AI 補助は「形を整える」だけに限る。URL を貼れば要約、スクリーンショットを貼れば文字起こし、画像は整形。
  **答えを作らせない。補助で入った文は既定で ❓（未検証）とし、✅ にできるのは出典を伴う行だけ**。
  検証済みのサブスクと同じ見え方で描くからこそ、未検証の行が同じ確信度に見えてはいけない
- v1 はページの新規作成のみ（既存ページの編集は Notion 側で行う。OAuth の権限は読み取り＋挿入まで確認済み、更新は未確認）
- 既存の土台: `/api/notion/create-cq`（利用者のトークンで自分の DB にページを作る）、`/api/notion/create-db`、
  複製テンプレ（設定の `notionDuplicatedTemplateId`）。CQ Catcher（別アプリ）の捕捉経路と役割が重ならないよう、設計で線を引く
- 読む側: 既存の降格式リーダー（`isInAppReaderTarget`・`/api/personal/page`）を既定のまま残し、テンプレ規約に沿うページだけ昇格させる。
  昇格の判定と、昇格したときの見え方（スプレッドとの共通部品）
- 配るテンプレ（複製元）の更新。教育はしない。フォームが規約どおりに書くので、教育の代わりになる
- AI 補助の費用と、画像の Notion への保存方法（ファイルアップロード API の可否）は設計で確かめる（未確認）

**部品8 改訂の旗（鮮度）**（設計セッションで決めること）
- 内の輪の骨格の5段目「鮮度（改訂で旗）」を担う。主張の本文が改訂されると ID が変わり旧 ID は `active=false` になるが、
  それを残していた人には何も届かないのが現状
- 決めること: 旧 ID と新 ID の対応の取り方（同じページ・同じ節・近い本文）、残していた人への旗の見せ方（球の色か印か）、
  カードの差し替えと SRS の記録（段・間隔）の引き継ぎ、出典台帳側で原著の撤回・懸念表明・改訂版を拾う口
- 参考の型: AnkiHub（承認制で全購読者へ自動配布）、scite Reference Check（撤回・懸念表明の通知）

## 今の時点で固定する継ぎ目

1. **主張の鍵は1つ。** `claimIdOf(ページID, 本文)`（sha1 先頭24文字。`extract-claims.ts` に実装済み）。
   受付DBの紐づけ列、Notion に落とす行の「MediNode ID」、段0の検索結果、すべてこの値を使う。
   他の部品が独自の ID を作らない。本文改訂で ID が変わる件は Recall 設計の `active=false` 方式に従う
2. **「残した」の記録は `recall_progress` の1本だけ。** 読む画面、球、段0の結果、依頼の正本化、どこから残しても同じ行。
   Notion への落としはこの表の変化を写す一方向
3. **「読んだ」は節ごとの明示操作。** `recall_section_reads` に `sec{n}` で記録する。
   既存の端末ローカルの読了水位とは統合しない
4. **受付DBに足す列は4つ**（2026-09-05 に「見送りの理由」を加えて3→4。部品3の設計書が正）。「段0結果」（select: 該当あり／該当なし／未実行）、「段0主張ID」、「正本主張ID」
   （どちらも rich_text。読む側はカンマ区切りを配列に、**正本主張IDの書き手は1件だけ積む**。2026-09-05 裁定3）、「見送りの理由」（select）。既存の列は変えない。
   列が無い受付DBでも既存動線が壊れないこと（`cq-submit` の `buildIntakeProperties` は無い列を積まない）
5. **正本化の合図は「対応状態＝対応済み」かつ「正本主張ID が空でない」。** 通知 cron がこの2つを見て、
   メールとプッシュを送り、飛び先は着地画面（疑問・回答＝主張・根拠・「残す」）。
   **cron は依頼者の `recall_progress` に書かない。「残した」は依頼者が着地画面の「残す」を押したときだけで、
   既存の keep API が間隔1日・期限翌日で始める**（2026-09-05 提案006の裁定1で書き換え。
   旧版の「`due_at` null で自動で残し、初回閲覧から期限を始める」は、`due_at` が `not null` であることと
   `remainingOf()` が `due_at` を見ないことの両方に合わず取り下げた）。正本主張ID が空なら従来どおりメールだけ。
   メール文面に Recall の名前を出さない
6. **機能フラグは `recall`（既存）と `ask_shelf`（新設）。** どちらも専用の許可メール（`RECALL_EMAILS`／`ASK_SHELF_EMAILS`）で、
   フォールバック無し。部品1は `recall` の内側、段0・段1・依頼は `ask_shelf` の内側。判定はサーバーが正で、
   費用が出る段1は閉じていれば処理に入らせない。全員開放の GA env は公開判断まで置かない
7. **段0は `recall_claims` を引き、次の3つを必ず満たす。**
   (a) 検索用の列は同じ表に足し、別の倉庫を作らない。
   (b) **利用者自身が残した主張（`recall_progress` に有効な行がある主張）を最上位に返す**。
   (c) 各主張に「この節を読む」を置き、読む画面の該当節へ跳べる（主張を新しい行き止まりにしない）。
   段0が該当なしのときの「棚に無い」の1行は外の輪の設計のとおり
8. **検索タブの見せ方。** 窓は今のまま。主張があるときだけ上に主張の段が開き（折りたたみ可）、下に今までどおりのページ。
   CQ 投げ込みのフォームは独立した入口をやめ、結果の末尾の「作者に検証を依頼」になる。
   節末の「気になる」は読む画面に残す。タブの構成（検索・新着・ジャンル・文献・クイズ・マニュアル・Recall）は変えない。
   **再検討ライン（2026-09-06 追記）**: オーナーが部品3を一周し、操作に迷った箇所を3つ以上数えたとき、または drnode.com の公開判断が先に来たとき。
   それまでは持ち出さない。ChatGPT 提案003（7タブ→検索／ライブラリ／Recall の3分類）は 2026-09-06 に「継ぎ目を開けない」と裁定済みで、
   比較と中間案は Notion「提案003 比較」に保管してある。開けると決めたときの入力にする
9. **無料／有料の線。** 主張の本文は有料。無料の利用者に段0が返してよいのは、題名・節名・件数と「棚に無い」の1行まで。
   オーナー専用の期間は判定を実装しなくてよいが、公開時に実装が要ることを段0の設計に書く
10. **「残す」に一括操作を置かない。** 71行を一括で残せると、2026-07 の「監修ナレッジ全文配布の却下」に実質抵触し、
    翌日71枚の宿題にもなる。API は既存のレート制限に載せる
11. **Notion に落とす行の列。** 主張／出典／分野／残した日／自分の一言／状態／MediNode ID（＝主張の鍵）。
    利用者側の編集は読まない（v1）
12. **テンプレの書き方の規約は、主張抽出とスプレッドが既に頼っている規約と同じにする。**
    番号付き H2 の節、✅⚠️❓の主張行と出典、⚡結論。部品7の記載フォームはこの規約どおりのブロックを書き、昇格描画はこの規約で判定し、
    v2 で自分のページの主張を球に出すときも同じ抽出器を使う。**規約の正は1か所**（`extract-claims.ts` の判定と、それを説明する文書）で、
    フォーム・描画・抽出が別々の規約を持たない。フォームの AI 補助で入った行は既定で ❓ とし、出典を伴う行だけ ✅ にできる
13. **今日の1問は部品6まで残す。** 撤去範囲は Recall 設計書の表のとおり
14. **見た目は部品0の `DESIGN.md` を基準にし、一新は凍結する**（次節）
15. **情報ごとの正本と同期方向は下の表で固定する**（提案010 判断5・2026-09-06）。部品はこの向きを変えない

### 情報ごとの正本・書き手・流れ（2026-09-06 追記）

提案010「Notionとの関係」の比較で、現行の構造を起こしたもの。**現行がすでにこの形**であり、新しい設計ではない。
聞ける棚 設計書 §8 の「情報ごとの管理主体」の表と対で読む（あちらは誰が書くか、ここは正本がどこにあり、どちらへ流れるか）。

| 情報 | 正本 | 書き手 | 流れ |
|---|---|---|---|
| 記事・節・主張の本文、確信度マーク、参考文献 | 作者 Notion | 作者 | Notion→Algolia／`recall_claims`／リーダー（毎晩 cron ＋ /admin）。一方向 |
| 主張ID・節キー | `recall_claims` | サブスク同期だけ | 生成のみ。Notion へ書き戻さない |
| 依頼の原文・投稿時点の職種・経験年数・診療科 | 受付DB | アプリ（投稿時1回）。控えを `cq_submissions` | アプリ→Notion 一方向 |
| 対応状態・ボード公開・見送りの理由・正本主張ID | 受付DB | 作者（/admin から） | /admin→Notion 一方向。アプリは読むだけ |
| 利用者に見える依頼の4状態 | 計算値 | アプリ | Notion→アプリ（読むたび） |
| 残した・読んだ・確かめた・間隔・期限 | Supabase | 本人（＋`srs.ts`） | Notion と無関係。部品4で Supabase→個人 Notion 一方向（計画） |
| 自分用 CQ | 個人 Notion | 本人（`create-cq` 経由） | アプリ→個人 Notion 一方向。**部品4で MediNode 側にも正本を持つ**（判断3） |
| 個人・部署の知識 | 個人・部署 Notion | 本人・部署 | Notion→Algolia（パワーモード）か Notion 直接検索。書き戻し無し |
| 票・気になる・投稿の控え・スプレッド・閲覧数 | Supabase | 本人／作者 | Notion に書かない |

2主体が同じ列を書くのは「対応状態・ボード公開」だけで、どちらも作者（/admin と Notion の手作業）。**利用者と作者が同じ列を書く箇所は無い。**
書き込みの原則: **MediNode 内の保存を成立させてから Notion に書く。** 現行で逆順なのは作者へ送る CQ の1か所だけで、判断1で塞ぐ。

## 見た目の扱い

- **見た目の方向は未決。** 凍結ではない（2026-09-03 オーナー訂正）。候補は2つ。現行（Noto Sans JP・常盤 #196B4F・lucide）を保つ案と、
  Node Field 統合LPのスタイルに寄せる案。常盤色は両案で残す前提。
  Node Field 側の現在の顔は「紺×白×群青・角丸なし・Archivo／Zen Kaku Gothic New／IBM Plex Mono・写真はデュオトーン・罫で継ぐ索引」で、
  2026-08-27 には MediNode と意図的に別の顔にすると決めていた。寄せる案を採るなら、この判断を開け直すことになる。
  常盤をこのパレットの中でどう置くかは、比較キャンバスで実物を並べて決める
- 方向が決まるまでは `DESIGN.md`（部品0）を現行の写しとして置き、新しい画面はそれに合わせる。方向が決まったら `DESIGN.md` を差し替え、
  以後の部品はその版に合わせる。既に作った画面の置き換えは棚卸しの1件として扱う
- 決める場は、部品3まで着地してオーナーが一周したあと。全画面の棚卸し（brushup。読み取り専用で候補7件、1件ずつ承認）と、
  同じ3画面（ホーム・読む画面の主張行・Recall）を現行案と Node Field 寄せ案で並べた設計キャンバスを作り、見比べて決める
- 決めるのを前倒しする条件: 一周の実測でオーナー自身が操作に迷った箇所が3つ以上出たとき、または drnode.com の公開判断が先に来たとき

## 議論で見つけた矛盾と処置（2026-09-03）

| # | 矛盾・弱点 | 処置 |
|---|---|---|
| 1 | 正本が戻ったとき、字義どおりだと初見が伏せ字カードになる | 継ぎ目5。自動では残さず、着地画面で「残す」を押した時点から間隔1日（2026-09-05 に `due_at` null 方式から書き換え） |
| 2 | 段0の主張が新しい行き止まりになる | 継ぎ目7(c)。「この節を読む」必須 |
| 3 | 棚が小さいあいだ段0はほぼ「無い」と答え、段1が実質の入口になって AI 検索と同じ土俵に立つ | 段1はオーナー専用で実測。継ぎ目7(b) で自分の残した主張を最上位に |
| 4 | 今日の1問と朝の通知を廃止すると、内の輪を外から引くものが無くなる | 部品6に週1の便りを同梱（日次にしない） |
| 5 | 段0の無料／有料の線が未定義 | 継ぎ目9 |
| 6 | 依頼の待ち時間に期待値が無く、投げっぱなしが再発する | 受付画面に期待値の1行、「対応不要」の理由表示。部品3で文言を決める |
| 7 | 「残す」の一括操作が凍結事項と Anki 地獄の両方に触れる | 継ぎ目10 |
| 8 | 想起カードは同じ節の主張が同じ表になり見分けられない | Recall セッションへ申し送り（冒頭の一句を手がかりに出す案）。本設計の外 |
| 9 | 「この節を読んだ」を押す動機が弱い | 矛盾ではなく賭け。一周の実測で押したかを見る |
| 10 | 一般利用者には、公開まで今日の1問が消えるだけの期間ができる | 実測で利用がほぼ無いため受け入れる。部品6を公開判断と同時にする選択肢は残す（オーナー判断） |

## セッションの回し方

- 1部品＝1セッション。順番どおりに進め、同じ main に2本を走らせない
- 設計が要る部品（3・4・5・7）は brainstorming → 設計書（`docs/superpowers/specs/`）→ 実装計画（`docs/superpowers/plans/`）。
  要らない部品（0・1・2・6）は実装計画から入る
- 実装はサブエージェント方式（実装 Sonnet 5・レビュー Opus 5）。worktree 必須
- テストは自作の文字列でなく実データを通す（`.preview/` の主張コーパス、受付DBの実物、本番の cron の手動実行）
- push は毎回承認。公開リポジトリなので事業数値を書かない
- 完了条件は「成功条件の該当行をオーナーが本番で実測した」。実装が終わっただけでは閉じない
- 引き継ぎは `~/.claude/handoff/app.md`（非公開）と Notion「MediNode アプリ開発」の申し送り欄

**着手条件**

| 部品 | 始めてよい条件 |
|---|---|
| 0・2 | いつでも |
| 1 | Recall でサブスク同期を1回押して `recall_claims` に主張が入り、残す→確かめる→覚えた の一周が本番で通っている |
| 3 | Recall セッションが同期側（主張の抽出・穴・承認）のコードを触り終えている |
| 4 | 部品3で「正本主張ID」の書き手が決まっている |
| 5 | 部品3が本番で動いている |
| 6 | 部品1〜3をオーナーが一周した後 |
| 7 | 部品1の後。/admin の個人・部署リーダーの計測を見てから。書く側と読む側は同じ設計セッションで扱う |
| 8 | 部品3の後（同期側と出典台帳側の両方に触るため）。実際に改訂が1件起きたときを目安にする |

## Recall セッションへの申し送り

- 想起カードに冒頭の一句（最初の読点まで）を手がかりとして出す案（矛盾8）
- ~~`due_at` が null の `recall_progress` 行を「残したが未開封」として扱えるようにする~~ **取り下げ（2026-09-05 提案006の裁定1）。**
  cron が `recall_progress` に書かなくなったので、この状態は生まれない

## 本設計で決めないこと

- 段0の検索方式、正本主張ID の書き手、受付画面の文言（部品3）
- Notion DB の作成の流れと API 世代（部品4）
- 段1のモデル・費用・保存先（部品5）
- クイズタブの端末記録の扱い（部品6）
- 記載フォームの項目と AI 補助の範囲（音声入力を含む）、昇格の判定と見え方、テンプレの更新内容、CQ Catcher との線引き（部品7）

## 参考にした外部サービスと、反映した場所

| 参考 | 借りたもの | 反映先 |
|---|---|---|
| NewMemo（AI 日記） | 声や写真を貼るだけで AI が形を整える | 部品7の記載フォーム（URL の要約・スクショの文字起こし・音声）、部品3の音声入力 |
| NewMemo | 自分の過去の記録に自然文で質問できる | 部品3の段0（自分が残した主張を最上位に返す） |
| NewMemo | 週報・傾向の分析 | 部品6の週1の便り（AI の作文は使わず、数字と区画名だけ） |
| NewMemo | AI の性格を選べる、AI が温かく振り返る | 借りない（キャラ常駐・多人格コーチは却下済み） |
| Refero Styles | 製品ごとの `DESIGN.md` という形式 | 部品0（中身は現行トークンの写し。他社の色や書体は入れない） |
| UI Pocket（Pro） | 「フロー」軸の実例（追加・作成／リクエスト/承認／連携・接続／クイズ・診断） | 各部品の画面を作る直前に、該当フローの実例を3画面だけ見る。設計段階では見ない（語学アプリが多く方向がぶれる） |
- 段0の無料判定の実装時期（公開判断のとき）

## 再検討ライン

- 見た目の一新: 上記「見た目の扱い」のとおり
- 部品の順番: 部品1の着手条件が 2026-10 を過ぎても満たされないとき、部品0・2・3 を先に進める順へ組み替える
- 週1の便り: 4週続けて開封が無ければ、便りの中身を見直す（廃止ではなく中身の見直し）

## パーツ調査（2026-09-03・4本の読み取り調査を骨組みに当てはめた結果）

調べた範囲: 想起・定着（A）、拾う・書く・Notion 落とし（B）、聞ける棚（C）、個人開発と国内の学習・知識アプリ（D）。
各係は実ページで確認した機能だけを報告し、確認できないものは「未確認」と書いた。ここでは骨組みに効くものだけを残す。
価値の判定は「サイクルのどこを良くするか」と「却下事項（毎日型ストリーク・ポイント・キャラ常駐・選択肢式・AI 作成のクイズ文・実名コミュニティ）に触れないか」で行った。

### 骨組みを変えたもの

- **部品8「改訂の旗」を追加する。** 内の輪の骨格（2026-09-01）は「読む → 拾う → 回す → 体得 → 鮮度（改訂で旗）」の5段で、
  骨組みには鮮度の部品が無かった。AnkiHub は監修デッキの修正を承認制で全購読者へ自動配布し（実ページ確認）、scite は引用先が撤回・懸念表明になるとメールで知らせる。
  MediNode の主張は本文改訂で ID が変わり `active=false` になるだけで、残した人に何も届かない。
  部品8: 改訂された主張を残していた人に旗を立て、球のカードを新しい主張に差し替え、SRS の記録（段・間隔）は保つ。原著の撤回・改訂を出典台帳側で拾う口も併せて決める。
  着手条件は部品3の後（同期側と台帳側の両方に触るため）
- **継ぎ目7(b) に印を足す。** 段0で自分が残した主張を最上位に返すだけでなく、「あなたが残した」の印を付ける（Semantic Reader が自分のライブラリに関係する引用を見た目で強調している型）
- **継ぎ目12 に見た目の印を足す。** 記載フォームの AI 補助で入った行は ❓ に加えて、AI 由来と分かる記号を前置する（Readwise Ghostreader は AI 出力をノート欄に隔離し、AI 由来のタグに記号を前置している。
  調査の範囲で「AI が入れた」を保持していた製品はこれだけだった）

### 部品ごとに借りるもの（設計セッションの入力にする）

| 部品 | 借りるパーツ | 出どころ（実ページ確認） | 価値 |
|---|---|---|---|
| 1 残す | 長押しで残す（ボタンを探させない。片手・移動中） | Matter の「highlight without friction」 | 中 |
| 3 段0 | 近い主張を「無理に出さない」閾値を数で決める。類似度が足りなければ出さず、「棚に無い」を言う | Consensus Meter（原文の抜き書きだけで作り、関連論文5本未満なら出さない） | 高 |
| 3 段0 | 「棚に無いときは答えない」をヘルプに仕様として書く | Notion Q&A（一般知識にアクセスしない）、NotebookLM（ソースに無ければ答えない） | 高 |
| 3 段0 | 主張の横に検証者と検証日を出す（誰が引き受けた主張かを名指しできることが差） | Doximity Ask PeerCheck が1.2万人の集合で同じ絵を持つ。差は「1人に固定」 | 中（検証日のデータ有無は未確認） |
| 3 段2 | 依頼画面の期待値は「平均○日・正本化率○%」の枠にし、実測が貯まったら数を入れる | Antaa QA（平均15分・回答率98%以上）、MedPeer（回答率95%以上） | 高 |
| 3 段2 | 板の説明の1行「一人の疑問は多くの疑問かもしれない」。投票の意味を説明する文 | MedPeer FORUM | 中 |
| 3 入口 | 音声で疑問を入れ、あとから文字と出典に戻れる | OpenEvidence Voice Mode、NewMemo | 中 |
| 4 Notion | 自動作成した DB の `data_source_id` を取得して保存する（2025-09-03 版は後方互換なし） | Notion API upgrade guide | 必須 |
| 4 Notion | Notion 未接続の人にも出口を置く（残した主張の Markdown 書き出し） | Glasp（手動コピーと自動 API の併存）、Recall（Markdown 書き出し） | 中 |
| 5 段1 | 引用は文単位。主張由来／出典はあるが位置が特定できない／由来なし の3段階で表示する | Elicit（sentence-level citations）、NotebookLM（ソースが短いと文単位でなく文書全体を参照と明示） | 高 |
| 6 便り | 便りは「メール」と「アプリ内の棚」に分け、メールを止めても中身は残る。曜日を選べる | Readwise（Email Frequency を Never にしてもアプリ内で読める。Themed Review は曜日指定） | 高 |
| 6 傾向 | 傾向の数字は「球のうち暗い主張の割合」「狙った定着率に届いているか」。AI 作文なし | Anki の FSRS 統計（Retrievability の分布・True Retention） | 高 |
| 6 傾向 | 弱い領域を診断名で名指しせず、「まだ」が続く主張そのものを並べる | Duolingo Practice Hub の Mistakes（未確認：実ページ未取得） | 中 |
| 6 便り | 当直・オンコールの曜日に期限を寄せない設定 | Anki の Easy Days | 中 |
| 6 便り | 便りに「偶然の1行」（残した主張から無作為に1つ）を添える。順序の最適化ではなく再会の驚き | mymind の Serendipity、Napkin の Daily Flow、RandoMemory（Notion のページを無作為表示） | 中 |
| 7 フォーム | 必須欄は主張と出典だけに絞り、分野は自動候補にして分類を求めない | Tana のスーパータグ（必須欄を絞り残りは任意）、mymind・Napkin（分類させない） | 高 |
| 7 フォーム | フォームの答えを本文テンプレの穴に差し込んで規約ブロックを組む | Tally → Notion（`@` で回答を page content に差し込む）。Notion Forms はプロパティしか書けず不採用 | 高 |
| 7 フォーム | 「Notion DB をフォームに変え、保存の瞬間に列を埋める」同型の既製品を参照する | Save to Notion | 高 |
| 7 フォーム | 整形の強さを選べ、「整形なし」を常に選べる | Cleft の Clean Transcript | 中 |
| 8 旗 | 承認制で全員に修正を配る。撤回・懸念表明の通知 | AnkiHub、scite Reference Check | 高 |

### Recall セッションへの申し送り（球と確かめるの改良。骨組みの外）

- **明るさを「思い出せる確率」にする。** FSRS の保持率 R(t) = (1 + (19/9)·t/S)^(-1) を、S に現在の間隔日数を入れて計算する。段は固定のままで、状態層の式だけが変わる。
  離脱候補の閾値もこの値で引き直す。価値: 高
- **「覚えた／まだ」を3択にする案。** Readwise Mastery は soon（7日）／Later（14日）／Someday（28日）の3択で、すべて日数ラベル。自己採点の語彙を使わない。
  「もう一度／来週／しばらく先」の形なら選択肢式クイズとは無関係のまま情報量が増える。価値: 高
- **伏せ字の量を記憶度で変える。** 暗い主張は数値1つだけ、明るい主張は上限3まで。原文の伏せ方だけ変えるので AI 作成には当たらない（Monoxer の段階化。出題形式の記述は未確認）。価値: 高
- 1日最大5枚の上限を、内部の「目標保持率」1つのノブに置き換える案（Anki の Desired Retention、既定90%）。実測後。価値: 中
- FSRS を全面採用はしない。個人最適化には数百回以上のレビューが要り（Anki マニュアル）、読者1人ではそこに届かない。固定段には SM-2 のような病理が無く、置き換えの得が小さい

### 借りないと決めたもの（理由つき）

- OpenEvidence の EvidenceGrade（AI が根拠の強さを採点）。「信頼できる AI 検索」の売り方そのもので、競合調査の結論に反する
- 選択肢式と誤答の自動生成（Quizionary・Recall の選択形式）。却下済み
- 学習ヒートマップ・連続記録（Anki Review Heatmap・Screvi のストリーク）。却下済み
- 実名 Q&A（Antaa QA・ヒポクラ）。却下済み。借りるのは期待値の数と工程だけ
- Notion Forms。本文ブロックを作れない
- 「整理しない」思想の全部（mymind）。出典必須は崩さない。借りるのは分類を求めないことだけ

### 調査で確認した MediNode の独自性

主張1行を検索の単位にする製品は既にある（scite の Citation Statement Search、Consensus の抜き書き）。ただしそれらの1行は「論文にそう書いてあった」記録で、人が正しさを引き受けた主張ではない。
Doximity は人が引き受けるが1.2万人の集合で、誰が引き受けたかは特定できない。
MediNode の1行も論文から引いた記述で、行為としては同じである（オーナー自身の指摘、2026-09-03）。差は人が特別なことではなく、
**工程が見えること**にある。全行に出典、確信度の印、層2と層3の照合の来歴、訂正の履歴。scite の引用文は機械が拾い、誰も読んでいない。
MediNode の1行には誰かが読んで確かめた跡が残る。差はそこまでで、棚が小さいうちは細い差でもある。
だから旗は「確かめてある」ではなく「二度目は、調べなくていい」のままにし、段0では「無いときに生成で埋めず、棚に無いと言い切る」ことを守る。

### X の検索（2026-09-03・ログイン済み Chrome で追加調査）

検索エンジンは X の中をほとんど見られないため、上の4本の調査に X の投稿は含まれていなかった。ログイン済みの Chrome で
「個人開発 暗記」「個人開発 Notion 連携」「個人開発 医療 医師」「個人開発 間隔反復」「#buildinpublic spaced repetition」と
ブックマークを見た。収穫は少なく、次の2件だけが骨組みに関係する。

| 見つけたもの | 何か（実ページ確認） | 反映先 |
|---|---|---|
| Rembly（rembly.app・個人開発） | FSRS で「忘れる直前」を予測するフラッシュカード。任意の文からカードを AI 生成 | 「忘れる確率」を前面に出す見せ方の先例。カードの AI 生成は借りない |
| emilkowalski/skills（GitHub・オーナーのブックマーク） | Claude Code 向けの設計スキル集。`apple-design`（Apple の設計原則と動きを Web 向けに翻訳）、`animation-vocabulary` | 部品0と見た目の比較キャンバスで、実装セッションに読ませる候補 |

「個人アプリ」の一語で探し直したところ（オーナーの助言）、医療系の個人開発が3つ見つかった。いずれも症例シミュレーションで、
MediNode とは学習の形が違うが、国内の医療者向け個人開発が実際に何をして人を集めているかの現物として記録する。

| 見つけたもの | 何か（実ページ確認） | 観察 |
|---|---|---|
| 内科病棟管理シミュレータ（個人・netlify） | 電子カルテ風に42症例をオーダーしながら管理。日替わり症例・ランキング・偏差値・症例の提案フォーム | 「本症例の現病歴・検査値・評価項目・解説は AI によって生成され、専門医の監修を経ずに掲載」と冒頭で明示。MediNode と正反対の立場を正直に書いている |
| Pocket-ER（ポケット救急外来） | 初療室の時間軸で症例を進める。今日の症例・学習分析・実績バッジ・総合ランキング・1日の無料回数 | 毎日型・バッジ・順位の3点セット。MediNode が却下したものが揃っており、対照として有用 |
| Clinical Pocket | 救急264・看護病棟47・総合内科外来56の症例を1つの器で。疾患・薬剤DBを併設。「得点や順位は臨床能力を認定しない」と明記 | 3職種を1つのアプリで受ける構え。MediNode の「看護／薬剤／CE 向け9CQ」と同じ広げ方 |

観察から骨組みに足すもの: 症例シミュレータは解説の根拠を持たない（AI 生成・未監修）。MediNode の検証済みの主張は、こうしたアプリの「解説の根拠」として
外に出せる（段0の API を第三者アプリに開く形）。公開判断の後の話であり、本設計では接続点の候補として記録するだけにする。

X は機械で探す場所として効率が悪いが、「個人アプリ」の一語のように短い語で「話題のポスト」を見る方が、複合語より当たる。
個人開発アプリは、オーナーが X で見つけてブックマークしたものを定期的に拾う方法と併用する（NewMemo はその経路で見つかった）。

## 仕組み台帳（アプリ単位ではなく、仕組み単位で取り出したもの・2026-09-03）

上のパーツ台帳は部品ごとに並べたが、アプリを丸ごと「当たる／当たらない」で判定していた箇所があった。
ここでは見たもの全部から仕組みだけを抜き、アプリ名は出どころとして添える。判定は「MediNode でどう使うか」まで書いたものだけ「借りる」にする。

### 入口（疑問・素材を入れる）

| 仕組み | 出どころ | MediNode での形 | 判定 |
|---|---|---|---|
| 声で1分話すだけで記録になる | NewMemo、AudioPen、Cleft | 検索タブの窓と記載フォームに音声。AI は疑問文の形に整えるだけ | 借りる（部品3・7） |
| 紙や画面を写真に撮るだけで取り込む | NewMemo、Screvi（紙の本の写真）、Napkin（スキャン） | 記載フォームのスクショ文字起こし | 借りる（部品7） |
| 取り込み口を4経路持つ（アプリ・共有シート・拡張・Web） | Napkin、reminDO | 記載フォームは「共有シート」を最初の追加経路にする（現場のスマホで一番速い） | 借りる（部品7の設計項目） |
| 整形の強さを選べ、「整形なし」を常に選べる | Cleft の3出力形式 | 音声・スクショの取り込みに「そのまま」を既定で残す | 借りる（部品7） |
| 貼った URL を要約して保存 | Recall.it、Screvi、NewMemo | 記載フォーム。要約は ❓ 既定 | 借りる（部品7） |
| 任意の文を貼るとカードになる | Rembly、Monoxer | v2（自分の主張を球へ）。v1 では借りない | 保留 |

### 拾う・残す

| 仕組み | 出どころ | MediNode での形 | 判定 |
|---|---|---|---|
| 長押しで印を付ける。ボタンを探させない | Matter | 読む画面の「残す」は長押しでも効く | 借りる（部品1） |
| 聞きながら印を付ける（音声ハイライト） | Matter | 読み上げは無いので借りない | 借りない |
| タグ・フォルダを作らせず、機械が寄せる | mymind、Napkin | 記載フォームで分野を求めない。自動候補にする | 借りる（部品7） |
| 保存の瞬間に列を埋めさせる（DB をフォームに変える） | Save to Notion | 記載フォームの土台の型 | 借りる（部品7） |
| 回答を本文テンプレの穴に差し込む | Tally→Notion | 規約ブロックの組み立て方 | 借りる（部品7） |
| タグを付けた瞬間に型の欄が生える。必須は少なく残りは任意 | Tana | フォームの必須欄は主張と出典だけ | 借りる（部品7） |
| 手動コピーと自動連携の併存 | Glasp | Notion 未接続でも Markdown で書き出せる | 借りる（部品4） |
| 一括で残せる | （多くのハイライト製品） | 置かない（凍結事項と宿題の山） | 借りない |

### 戻ってくる（開かせずに届ける）

| 仕組み | 出どころ | MediNode での形 | 判定 |
|---|---|---|---|
| 毎日1通のメールで再提示 | Readwise、Notionist | 日次にはしない。週1の便りをメールでも受け取れる | 一部借りる（部品6） |
| メールを止めてもアプリ内に同じ便りが残る | Readwise | 便りをメールと棚に分ける | 借りる（部品6） |
| 忘れかけた頃にだけ通知する | reminDO、Rembly | 「期限の来た主張が n 件」の週1通知 | 借りる（部品6） |
| 曜日を選んで負荷を寄せる・避ける | Anki Easy Days、Readwise Themed Review | 当直の曜日を避ける設定 | 借りる（部品6） |
| 忘れていたものを無作為に出す（偶然の再会） | mymind Serendipity、Napkin Daily Flow、RandoMemory | 便りに「偶然の1行」 | 借りる（部品6） |
| ホーム画面ウィジェットに自分のデータの一片を置く | Sheet Widget、Screvi | PWA では iOS のウィジェットが作れない可能性が高い（未確認） | 保留 |
| 日替わりの1問と、その日だけの挑戦者数 | 内科病棟管理シミュレータ、Pocket-ER | 日替わりは廃止済み。「同じ日に同じ主張を確かめた人数」だけを便りに添える案 | 保留（数を出す是非をオーナー判断） |
| 1日の無料回数を表示する | Pocket-ER（本日の無料 残り 10/10） | 段1の費用方式の候補: 1日 n 回まで無料、残りを表示 | 借りる（部品5の設計項目） |

### 確かめる・定着

| 仕組み | 出どころ | MediNode での形 | 判定 |
|---|---|---|---|
| 忘れる確率を予測し、明るさや順番に使う | FSRS（Anki・Rembly） | 球の明るさを R(t) にする | 借りる（Recall 申し送り） |
| 答え方を日数ラベルの3択にする | Readwise Mastery（soon／Later／Someday） | 「もう一度／来週／しばらく先」 | 借りる（Recall 申し送り） |
| 自信の度合いで回す（1〜5） | Brainscape | 5段階は多い。3択に畳む | 一部借りる |
| 記憶度に応じて出題の形を段階化する | Monoxer（未確認）、Rembly | 伏せ字の数を記憶度で変える | 借りる（Recall 申し送り） |
| 固定の初期段＋後半は適応 | RemNote | 段は固定のまま、明るさだけ適応 | 借りる（根拠として） |
| 目標の保持率を1つのノブにする | Anki Desired Retention | 上限5枚の代わりの内部ノブ | 保留（実測後） |
| 取りこぼしたものを、診断名でなくそのまま並べる | Duolingo Mistakes（未確認） | 「まだ」が続く主張の一覧 | 借りる（部品6） |
| 読んだ本が出題元になる | Loxie | 既に同じ設計 | 済 |
| 病態がランダムに急変し、対応も採点される | 内科病棟管理シミュレータ | 予期しない出題＝球からの離脱と同じ役。追加なし | 済 |
| 病日を進めるごとに点数が出る（即時の採点） | 同上 | 覚えた／まだ の即時反映と同じ | 済 |
| 採点基準を「ヒント」で先に見せる | 同上 | 確信度 ✅⚠️❓ の基準を1画面で見せる（検証の現場を見せる） | 借りる（部品3の段0結果画面から辿れる説明） |
| 連続日数・バッジ・順位・偏差値・季節制ランキング | 内科病棟管理シミュレータ、Pocket-ER、KikoSensei、Screvi | 却下済み | 借りない |
| 全体の平均点と自分の平均点を並べる | 内科病棟管理シミュレータ | 順位の一種。区画ごとの「まだ」率を自分の中だけで見せる | 借りない |

### 信頼を見せる・訂正を受ける

| 仕組み | 出どころ | MediNode での形 | 判定 |
|---|---|---|---|
| AI 生成・未監修を冒頭で明示する | 内科病棟管理シミュレータ、Clinical Pocket | 段1と記載フォームの「未検証」表示の文言の型 | 借りる（部品5・7） |
| AI の出力を人の記入と別の欄に隔離し、記号で見分ける | Readwise Ghostreader | AI 補助の行に記号の印 | 借りる（継ぎ目12） |
| 引用は文の単位で、出典の文に戻れる | Elicit、EvidenceHunt | 段1の引用チップ | 借りる（部品5） |
| 出典はあるが位置が特定できない、という中間状態を正直に出す | NotebookLM | 段1の由来表示を3段階に | 借りる（部品5） |
| 棚に無ければ答えない、をヘルプに仕様として書く | Notion Q&A、NotebookLM | 段0の「棚に無い」 | 借りる（部品3） |
| 抜き書きだけで判定を作り、材料が少なければ出さない | Consensus Meter | 段0の閾値 | 借りる（部品3） |
| 自分のライブラリに関係する引用を見た目で強調する | Semantic Reader | 段0の「あなたが残した」の印 | 借りる（継ぎ目7） |
| 引用先が撤回・懸念表明になったら通知 | scite Reference Check | 部品8。出典台帳の口 | 借りる（部品8） |
| 監修デッキの修正を承認制で全員に配る | AnkiHub | 部品8 | 借りる（部品8） |
| 「症例の内容に誤りがあった」を種別つきで投げられるフォーム | 内科病棟管理シミュレータ | 主張ごとに「この主張は違う」を投げる入口。訂正履歴に繋ぐ。段2の依頼に「訂正」の種別を足す | 借りる（部品3・8） |
| 版を更新しても「既にお使いの方はそのまま」と告げる | 社労士学習管理アプリ | 改訂の旗の文言の型（記録はそのまま、主張だけ新しくなる） | 借りる（部品8） |
| 「得点や順位は能力を認定しない」と明記 | Clinical Pocket | 傾向の表示に同じ断り | 借りる（部品6） |
| AI が根拠の強さを採点して見せる | OpenEvidence EvidenceGrade | 借りない（AI 検索の売り方） | 借りない |
| 多数の医師が AI 回答を検証する | Doximity PeerCheck | 借りない。差は「誰が確かめたかが分かる」に置く | 借りない |

### 依頼と作者の工程

| 仕組み | 出どころ | MediNode での形 | 判定 |
|---|---|---|---|
| 回答までの平均時間と回答率を数で出す | Antaa QA、MedPeer | 依頼画面の期待値の枠 | 借りる（部品3） |
| 「一人の疑問は多くの疑問かもしれない」 | MedPeer | 板の説明文 | 借りる（部品3） |
| 集まった問答を選抜し、回答者に再取材して正本にする | Antaa QA の書籍化 | 空白候補ビューの優先順位と、正本化の工程の型 | 借りる（部品3・制作工程） |
| 疑問を投げると専門科へ回る | ヒポクラ 他科コンサルト | 実名なので借りない | 借りない |
| 利用者が症例（素材）を投稿できる | Pocket-ER、内科病棟管理シミュレータ | 段2の依頼と同じ。追加なし | 済 |
| 匿名の名前だけで始め、後からログインで記録を引き継ぐ | Pocket-ER、内科病棟管理シミュレータ | 本番は意図的にログイン必須。公開時の無料窓で再検討 | 保留 |
| 3職種を1つの器で受け、対象・症例数・進み具合を職種ごとに出す | Clinical Pocket | 球のレンズ（職種で手前に出す）と同じ発想。追加なし | 済 |
| 参照 DB（疾患・薬剤）を訓練の横に併設 | Clinical Pocket | 文献タブと主張の関係と同じ。追加なし | 済 |
| 無料・広告なし・アカウント不要、を最初に言う | ねこ勤怠 | 「Notion 接続は任意」を最初に言う文言の型 | 借りる（公開時の文言） |
| 1つのボタンで、生きているものが動き出す | ねこ勤怠（猫が歩く） | 「残す」で球が灯る。既に同じ | 済 |

### この台帳から骨組みに足したもの

- 部品3の段2に**「訂正」の種別**（「この主張は違う」を主張ごとに投げられる入口）。部品8の改訂の旗と対になる
- 部品5の費用方式の候補に**「1日 n 回まで無料、残りを表示」**
- 部品6の傾向の表示に**「得点や順位は能力を認定しない」の断り**
- 部品7の設計項目に**共有シートを最初の追加経路にする**こと

## 中の仕組みの深掘り（証拠・推測・MediNode での実行可能性・使い所）

3本の技術調査（入口・拾う系／確かめる・戻す系／聞ける棚・信頼・医療シミュ系）で、公開情報（開発者記事・ヘルプ・特許・査読論文・
プライバシーポリシー・実サイトの HTML と JS）から各製品の裏側を集めた。ここでは仕組みごとに「証拠の確度」「中の仕組み」「MediNode での実装」
「実行可能性」「使い所」を書く。推測は推測と明記する。

### 判定の前提（MediNode の技術構成・確認済み）

Next.js（Vercel・hnd1・Pro）、Supabase Postgres（pgvector 利用可。PGroonga は拡張の有効化で使える）、Notion API は `@notionhq/client` 2.2 系で
2022-06-28 版固定、利用者の Notion への OAuth は読み取り＋挿入、メールは Resend、通知は Web Push、PWA（Service Worker あり）、レート制限は Upstash、
検索は Algolia（ページ単位）。**LLM と埋め込みの API キーは本番 env に無い**（段1・埋め込み・音声整形はここから足す）。

### 証拠の濃さ（先に断っておく）

| 濃い（仕様として読める） | 薄い（語っていない） |
|---|---|
| Cleft（端末内 Whisper・整形は OpenAI/Groq・音声は外に出さない）、Screvi（埋め込み＋コサイン・SM-2 改・Notion の block 種別・MCP 4ツール）、Readwise（半減期 7/14/28 日・閾値 50%・二部構成）、scite（GROBID→3文→SciBERT・F値公表）、Consensus（BM25＋ベクトル→リランク→分類器・誤分類 10%）、Monoxer（特許4件に記憶度の値）、医療シミュ3つ（実サイトの JS を読んだ） | mymind（「magic behind the scenes」）、Napkin（自社サーバの AI とだけ）、NewMemo（プライバシーポリシーに Gemini API・Supabase・ベクトル生成の3語。音声認識の提供元は不明）、Rembly（FSRS とだけ）、Recall.it（「proprietary」） |

### 仕組みごとの判定

凡例。実行可能性は MediNode の構成での実装難度と依存で「高＝既存部品で足りる／中＝新しい API か設計判断が要る／低＝土台工事が要る」。

**1. 声で入れて、疑問文の形に整える**
- 証拠: Cleft は端末内 Whisper（CoreML）で転写し、**転写テキストだけ**を OpenAI（予備 Groq）へ送って様式プロンプトで再構成。出力形式（Structured／Prose／Clean Transcript）はプロンプト差し替え。AudioPen は Whisper API＋GPT で書き直し、rewrite level を変数に持つ（二次情報）。NewMemo は Gemini で整形（プライバシーポリシー）。音声認識の提供元は NewMemo・AudioPen・Napkin とも一次情報なし
- MediNode での形: ブラウザの Web Speech API（Safari は Apple のサーバで認識）か、MediaRecorder で録って Whisper API（数円/分）。転写だけを Claude Haiku に渡し「疑問文の形に整える」1行プロンプト。**整形なし（転写のまま）を常に選べる**（Cleft の Clean Transcript）
- 実行可能性: 高。PWA で完結し、音声を自社サーバに置かない構成にできる（Cleft と同じ「音声は外に出さない」の約束が書ける）
- 使い所: 部品3の入口、部品7の記載フォーム。AI は形だけ、答えを作らない

**2. 自分の記録に自然文で聞く（NewMemo の「過去の自分に質問」）**
- 証拠: Gemini の埋め込み（「ベクトル生成」）＋Supabase。回答は Gemini が生成（推測を含む）
- MediNode での形: 段0は同じ入口で**生成を外す**。問いを埋め込み、`recall_claims` に足したベクトル列でコサイン近傍。自分が残した主張（`recall_progress`）を加点して最上位へ。返すのは主張の原文だけ
- 実行可能性: 高。700 主張の埋め込みは一度で済み、費用は無視できる。埋め込みの提供元（OpenAI 3-small／Gemini／Cohere embed-v4）は日本語の医学文で実測して選ぶ。参考: Screvi は同じ構成（埋め込み＋コサイン）を仕様として公開している
- 使い所: 部品3の段0

**3. 写真・スクショから文字を取り込む**
- 証拠: NewMemo は Gemini の画像解析（明記）。Screvi は「AI OCR」（エンジン名なし）。mymind は「重要な語を索引化」（全文ではない）
- MediNode での形: 部品7で画像を Claude か Gemini に渡して文字起こし。入った行は ❓ 既定。**Notion への画像保存は未確認**（File Upload API は 2022-06-28 版で使えるかを部品4・7の設計で確かめる）
- 実行可能性: 中（画像の Notion 保存の版問題が残る）。文字起こし自体は高
- 使い所: 部品7

**4. 週報・傾向・性格診断（NewMemo）**
- 証拠: 保存済み本文を Gemini にまとめて投げる定期処理（推測）
- MediNode での形: **LLM を使わない**。`recall_review_log`・`recall_progress`・`recall_section_reads`・`recall_claims` の SQL 集計だけで「今週確かめる n 件」「まだが多い区画」「棚にあって触れていない区画」を出す。Vercel cron → Web Push／Resend
- 実行可能性: 高。既存の cron・Push・Resend で足りる
- 使い所: 部品6。性格診断は借りない（区画の偏りを「関心の地図」として見せるまで）

**5. 分類させず、機械が分野を寄せる（Napkin・mymind）**
- 証拠: 両社とも手法は非開示。Napkin は自社サーバの AI と明言（推測: オープンな埋め込みモデル＋クラスタリング）
- MediNode での形: 分野は 34 席が定義済みなので、クラスタリングは要らない。記載フォームの本文を埋め込み、`recall_claims` の主張（席のラベル付き）で最近傍 k 件の多数決 → 分野の候補を1つ出す。利用者は直すだけ
- 実行可能性: 高。ラベル付きの実データ 700 件が既にある
- 使い所: 部品7

**6. 保存の瞬間に列を埋め、本文テンプレの穴に差し込む（Save to Notion・Tally）**
- 証拠: Tally は対応するプロパティ型を全部公開し、**Person と Relation は非対応**（UUID の解決が要るため）。本文は「Page content block」に回答を差し込む。Save to Notion は API に触れないが、抽出は JSON-LD／OGP のメタデータに依存していると読める（推測）
- MediNode での形: `pages.create` に `parent: {database_id}` ＋ `properties` ＋ `children`（規約ブロック）を1回で送る。URL からの自動抽出は OGP（題名・著者・日付）で足り、AI は要らない。Relation 列（出典 DB へのリレーション）は Tally と同じ壁に当たるので v1 は使わない
- 実行可能性: 高。既存の `/api/notion/create-cq` が同じ呼び出しをしている
- 使い所: 部品7、部品4

**7. Notion に「作り直さず追記する」同期と、読み取り専用の MCP（Screvi）**
- 証拠: Screvi は DB を自動作成し、ソース1件＝1ページ、ハイライトは quote ブロック、メモは callout、「既存ページは更新、再作成しない」、日次同期。MCP は OAuth 2.0＋PKCE で読み取り専用の4ツール
- MediNode での形: 部品4は 1主張＝1行（ページ）で、`databases.query`（MediNode ID で検索）→ 無ければ `pages.create`、あれば `pages.update`（状態列だけ）。**`data_source_id` を保存**（2025-09-03 版）。MCP の読み取り専用公開は、公開時の費用方式の候補（自分の棚を自分の Claude から引く）
- 実行可能性: 高（同期）／中（MCP は OAuth サーバが要る。SUBSCreen に OAuth 一式の参照実装あり）
- 使い所: 部品4／公開判断の後

**8. 二部構成の日次レビューと半減期（Readwise）**
- 証拠: 前半＝未処理ハイライトの確率抽選（文書の大きさで重み、出すたびに確率を下げる）、後半＝Mastery の想起確率が 50% 以下のもの。半減期の初期値 soon 7 日／Later 14 日／Someday 28 日。末尾に Bonus 1 枚。Themed Review は別スケジュール。減衰式と更新率は非公開
- MediNode での形: 週1の便りを同じ二部構成にする。前半＝期限の来た主張 n 件（`due_at`）、後半＝偶然の1行（残した主張から無作為 1 件。Bonus に相当）。半減期の考え方は Recall の明るさに使う
- 実行可能性: 高。純関数と SQL だけ
- 使い所: 部品6、Recall 申し送り

**9. 忘れる確率の予測（FSRS・Rembly）と端末内スケジューラ**
- 証拠: Rembly は FSRS を核にし、Flutter 製で `fsrs-rs-dart` を使っている可能性（推測）。カード生成だけサーバ、復習は端末で完結しオンライン時に同期（推測）
- MediNode での形: `ts-fsrs` は使わず、R(t) = (1 + (19/9)·t/S)^(-1) を状態層に置く（S は現在の間隔）。段は固定のまま。オフラインの確かめるは Service Worker に主張と記録を持たせ、復帰時に差分同期（後勝ちの衝突が残る）
- 実行可能性: 高（R(t)）／中（オフライン同期）
- 使い所: Recall 申し送り。オフラインは v2

**10. 記憶度に応じた出題の段階化（Monoxer）**
- 証拠: 特許 JP2025033035A に記憶度の値（正答 +20／誤答 −5、閾値 20 と 80、子の +20 に親は +10）。JP7495135B2 は「薄字ヒント → 5択 → 自由入力」の段階化と、解答時間・訂正回数からの定着度判定を請求。誤答生成は Word2Vec の**上位10件は同義語が 64% なので 11 位以降を使う**（技術ブログ）
- MediNode での形: 伏せ字の数を記憶度（R(t)）で変える案は、形式を変えるのではなく穴の数を変えるだけで、選択肢式も自由入力も使わない。**ただし JP7495135B2 の請求範囲に近い設計なので、実装前に請求項を読む**（未確認）。誤答生成は選択肢式を却下しているので不要
- 実行可能性: 高（穴の数を変えるのは `holes` の上限を r で決めるだけ）
- 使い所: Recall 申し送り（特許の確認つき）

**11. 期日のあるときだけ通知する（reminDO）**
- 証拠: 固定間隔 1／3／7／14／30 日。「復習すべきアイテムがある場合のみ通知」。適応型ではない
- MediNode での形: 週1の便りは、確かめる主張が 0 件で偶然の1行も無ければ送らない
- 実行可能性: 高
- 使い所: 部品6

**12. Notion の DB を読んで、期日のノートをメールで出す（Notionist）**
- 証拠: Notion DB 単位で接続、レビュー履歴で次回を決める、無料は1通5件、AI クイズは有料のみ（基本はノートをそのまま出す）
- MediNode での形: 部品6の便りをメールでも出す。本文は主張の原文と出典だけ
- 実行可能性: 高（cron＋Resend は既存）
- 使い所: 部品6

**13. Notion の公開インテグレーションの主キー（RandoMemory）**
- 証拠: OAuth 応答の `bot_id` を主キーにする（Notion 公式の推奨を引用）。Notion トークンをクライアントに渡さない
- MediNode での形: 既存の OAuth（かんたん接続）が `bot_id` を保持しているかを確認する（未確認）。部品4で DB の ID と一緒に持つ
- 実行可能性: 高
- 使い所: 部品4の設計項目

**14. 関連する考えを自動で結ぶ（Recall.it・Napkin）**
- 証拠: 手法は非開示（推測: 埋め込み近傍）
- MediNode での形: 段0で足すベクトル列をそのまま使い、主張の閲覧カードに「近い主張 3 件」を出す。生成なし
- 実行可能性: 高（段0の副産物）
- 使い所: 部品1のカード、部品3の段0

**15. 生成せずに抜き書きで答え、材料が少なければ出さない（Consensus・scite・Elicit・NotebookLM）**
- 証拠: Consensus は BM25＋ベクトル検索 → 被引用数・年でリランク → 上位 20 件を fine-tuned 分類器で yes/no/possibly、関連性と確信度の2閾値で足切り、誤分類 10% と公表。抜き書きは word-for-word。scite は F値が supporting 68%／disputing 59% で、適合率 80% 以上になるよう閾値で調整。Elicit は文単位の引用。NotebookLM は direct quotes で、**ソースが短いと箇所を特定できず文書全体を参照先にする**
- MediNode での形: 段0は分類器が要らない（確信度は人が付けている）。ハイブリッド検索（PGroonga＋pgvector）→ 類似度の閾値で「棚に無い」を判定。**AI による supporting／contrasting の分類は借りない**（2クラスで3〜4割外す）
- 実行可能性: 高
- 使い所: 部品3の段0

**16. 文単位の引用を API に任せる（Elicit・NotebookLM と同じことを Claude Citations で）**
- 証拠: Anthropic Citations API は文書を文単位に分割し、`cited_text`／`char_location`／`document_index` を返す（公式）。Gemini File Search も `file_citation` を返す
- MediNode での形: 段1は、段0で引いた主張を**1主張＝1文書**として Claude に渡し、Citations を有効にする。返答のうち引用付きの範囲が「主張由来」、引用のない範囲が「由来なし」。NotebookLM の劣化（短いソースは文書全体を参照）は、1主張を1文書にすることで回避する
- 実行可能性: 高（実装は薄い。費用は1問あたり数十円規模、上限は Upstash のレート制限で1日 n 回）
- 使い所: 部品5

**17. 撤回・訂正の照合を無料データで回す（scite Reference Check）**
- 証拠: scite の撤回判定は DOI を撤回・編集通知のテーブルに突き合わせる単純参照（推測）。Crossref が Retraction Watch のデータを買収して無料公開（REST API と CSV、営業日ごと更新）
- MediNode での形: 出典台帳の `doi:` キー（168 行）を週1で Crossref の撤回データと突き合わせ、当たった出典を持つ主張に旗を立てる。部品8の「出典台帳側の口」はこれで足りる
- 実行可能性: 高。cron 1 本と台帳の読み取りだけ
- 使い所: 部品8

**18. 症例を事前生成して埋め込み、採点は固定ルーブリック（医療シミュ3つ）**
- 証拠: 実サイトの JS を読んだ。3つとも症例をクライアントに埋め込み、採点関数は手書きの配点、**実行時の LLM 呼び出しは 0 件**。日替わりは日付のハッシュで既存症例を1つ選ぶ。内科病棟管理シミュレータは Supabase の leaderboard と device_id、Pocket-ER は Cloudflare Workers＋D1＋Clerk＋Turnstile と `/api/quota` の無料回数、Clinical Pocket は React Server Components
- MediNode での形: (a) 「偶然の1行」は利用者 ID＋週のハッシュで決定的に選ぶ（同じ週は同じ1行）。(b) 段1の無料回数は Upstash で1日 n 回。(c) **採点に LLM を使わない**のは3つとも同じで、MediNode の伏せ字も原文との一致（数値）と自己申告（覚えた／まだ）だけにする。(d) 症例の AI 生成は借りない（未監修を免責している）
- 実行可能性: 高
- 使い所: 部品5・6、Recall

**19. 診療科タグで通知を振り分け、テンプレとお礼で回す（Antaa QA）**
- 証拠: 通知は該当診療科の医師へ。15 分を支えているのは技術でなく運営（質問テンプレ・回答者へのお礼メール・配信での紹介）。ベストアンサーの選定主体は未確認
- MediNode での形: 作者は1人なので振り分けは要らない。借りるのは**質問テンプレ**（依頼フォームの「背景・状況」「何を調べたか」を必須に近づける）。正本化が速くなる
- 実行可能性: 高（受付DBに列はある）
- 使い所: 部品3の段2

**20. 検証者の名前と日付を回答の先頭に出し、査読済みの回答を同種の問いに再提示する（Doximity PeerCheck）**
- 証拠: 査読者の実名とプロフィールへのリンクが回答の先頭に出る。「when available」の条件付き表現から、査読済み回答を保持して再提示していると読める（推測）
- MediNode での形: 段1の答えは問いごとに保存し、後日その問いが正本化されたら**同じ問いには正本を先に出す**（外の輪の設計で未決だった「段1の答えが正本に置き換わったときの見せ方」の答え）。正本の主張には検証日と照合の有無（層2・層3）を出す
- 実行可能性: 高（保存は部品5、表示は段0）
- 使い所: 部品3・5

### 深掘りで分かった、設計判断に効く事実

1. **抜き書きを LLM に書かせている製品は無い。** Consensus・scite・Elicit・NotebookLM の4つとも原文を返す。段0の「生成しない」は業界の実務と一致している
2. **AI の分類は公表されている限り精度が低い**（scite の2クラスで F値 59〜68%、Consensus は誤分類 10%）。両社とも「確信が足りなければ分類しない」逃げ道を持つ。MediNode は確信度を人が付けるので分類器を持たず、段0の「出さない」閾値だけ持つ
3. **採点に LLM を使っている学習アプリは、調べた範囲に1つも無い。** 医療シミュ3つは固定ルーブリック、Readwise・Monoxer は自己申告か一致判定。MediNode の伏せ字も同じでよい
4. **音声は端末で転写し、テキストだけを外に出す**（Cleft）。MediNode の PWA でも同じ約束が書ける
5. **Notion 連携で必ず当たる壁は Person と Relation**（Tally）。部品4・7は v1 でリレーションを使わない
6. **Monoxer の段階化は特許で押さえられている。** 伏せ字の数を記憶度で変える案は、実装前に JP7495135B2 の請求項を読む
7. **内部を語らない製品（mymind・Napkin）から借りられるのは振る舞いだけ**で、仕組みは自分で決めるしかない。振る舞い（分類させない・偶然に再会させる）は上の 5 と 8 に落とした
